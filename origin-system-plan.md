# 出身系统完善计划

> 状态：已按本计划完成代码落地与测试验证；本文件保留为实现说明。

## 一、目标与边界

完善角色创建第 6 步的出身系统，使出身同时影响：

- 角色对家庭、魔法界和童年环境的稳定认知；
- 分院完成前，角色默认如何理解霍格沃兹来信、前往对角巷和入学流程；
- 模型在默认剧情与玩家主动选择发生冲突时的处理方式。

明确不改变：

- 四种剧情起点的选择、日期、地点和第一幕核心事件；
- 魔杖获得前置规则；
- 分院完成前置规则；
- 学籍、课程、技能和其他程序权威状态。

出身只提供叙事背景和角色认知，不获得额外的剧情起点，也不自动解锁魔杖或学院身份。

## 二、当前问题

当前实现存在以下耦合：

- 三种预设出身只有角色创建页面上的选项描述，没有单独的、可持续注入每轮提示词的基础介绍；
- 纯血、混血、麻瓜和自定义出身共用一段入学前认知规则，无法精确表达三种预设出身的不同默认流程；
- 入学前规则当前主要通过 `enrollment_started` 控制，而需求要求在角色完成分院后移除；
- 自定义出身虽然可以由模型推理，但没有明确的“默认收信→对角巷→霍格沃兹特快→学校”流程说明；
- 出身规则需要和四种剧情起点、魔杖规则、分院规则彻底分层，避免互相覆盖。

## 三、出身数据模型设计

新增统一的出身内容源，建议放在：

```text
backend/app/content/origins.py
```

每个预设出身包含以下字段：

- `id`：`pure_blood`、`half_blood`、`muggle_born`；
- `label`：前端展示名称；
- `setup_description`：第 6 步选项下的一句话介绍；
- `base_prompt`：每轮都注入的基础介绍词；
- `pre_enrollment_prompt`：分院前注入的默认背景提示词。

推荐的基础介绍词：

- 纯血：`来自巫师家庭，自幼熟悉魔法界的生活、礼仪与常识。`
- 混血：`家庭同时连接巫师社会与麻瓜社会，角色从小接触过两种生活方式。`
- 麻瓜：`父母都是麻瓜，角色在收到霍格沃兹来信前一直生活在麻瓜社会。`

推荐的分院前背景提示词：

- 纯血：
  - 已经熟知魔法界与魔法；
  - 会期待霍格沃兹来信；
  - 通常由家人带着前往对角巷购买入学物品。
- 混血：
  - 对魔法界与魔法有基础认知；
  - 会期待霍格沃兹来信；
  - 可由家人或麦格教授带着前往对角巷，具体根据当前剧情决定。
- 麻瓜：
  - 对魔法界与魔法毫无认知；
  - 不会期待霍格沃兹来信；
  - 来信应当是惊喜；回信后通常由麦格教授带着前往对角巷购买物品。

自定义出身不注入 `base_prompt`。自定义出身只注入独立的模型推理规则：

- 综合 `family.bloodline`、`family.description`、童年经历、角色补充和 `setup.answers` 判断角色是否知道魔法界；
- 判断结果在后续剧情中保持一致；
- 默认流程大体保持“收信→前往对角巷→霍格沃兹特快→学校”；
- 但默认流程不是强制剧本，玩家的明确行动和自由输入优先。

## 四、状态规范化

在角色状态物化时增加稳定的出身分类字段，建议使用：

```json
{
  "family": {
    "origin_id": "pure_blood",
    "bloodline": "纯血家族",
    "description": "你的家族背景将在故事中逐渐展开。"
  }
}
```

规范化规则：

- `pure_blood`、`纯血家族` 等预设值统一归一为 `pure_blood`；
- `half_blood`、`混血家庭` 统一归一为 `half_blood`；
- `muggle_born`、`麻瓜出身`、`麻瓜家庭` 统一归一为 `muggle_born`；
- 其他值统一视为 `custom`；
- 自定义原始文本仍保留在 `family.bloodline` 或角色补充信息中，供模型推理；
- 旧存档缺少 `origin_id` 时，根据现有 `bloodline` 运行时推导，不要求迁移旧数据库。

前端第 6 步仍只展示三个预设选项，并保留当前允许自由输入其他出身的能力。自定义输入不能被误归类为三种预设。

## 五、提示词分层方案

在 `backend/app/prompts/turn.py` 中拆成三个互相独立的层。

### 1. 出身基础介绍层

每轮都根据 `player_state.family.origin_id` 注入：

```text
当前角色的出身基础背景：……
这是一条持续有效的角色背景事实，后续叙事应保持一致，但不能覆盖玩家行动和程序权威状态。
```

只有三种预设出身注入该层。`custom` 不注入任何预设基础介绍词。

该层不因以下状态移除：

- 是否收到来信；
- 是否前往对角巷；
- 是否登上列车；
- 是否完成分院；
- 是否获得魔杖；
- 是否正式入学。

### 2. 分院前背景层

该层只在 `sorting_completed` 为 false 时注入，角色完成分院后立即移除。

预设出身注入对应的 `pre_enrollment_prompt`；自定义出身注入模型推理规则。该层必须明确：

- 这是默认情况下的剧情倾向，不是强制剧本；
- 玩家明确选择或自由输入的行动优先；
- 玩家选择可以改变陪同者、抵达方式、时间节奏和具体事件；
- 不得因为默认流程而替玩家制造没有发生的事件；
- 不得改写玩家选择的四种剧情起点；
- 如果玩家从对角巷、站台或分院起点开始，不应倒放此前被跳过的流程。

### 3. 与其他规则的组合顺序

建议顺序：

1. 通用叙事和状态权威规则；
2. 始终存在的预设出身基础介绍；
3. 分院前出身背景规则；
4. 四种剧情起点规则；
5. 魔杖可用性规则；
6. 分院可用性规则；
7. 课程、声望、羁绊和其他系统规则；
8. JSON 输出协议。

出身规则不得修改或替代魔杖、分院提示词，只能与它们并列存在。

## 六、四种剧情起点约束

出身规则必须服从 `player_state.current_context` 和 `activity`：

- `before_first_letter`：按程序设置的 1991-07-01 来信节点开始；
- `diagon_alley`：直接从第一次踏入对角巷的节点开始；
- `platform_nine_three_quarters`：直接从 1991-09-01 站台节点开始；
- `sorting_ceremony`：直接从分院仪式节点开始。

纯血、混血或麻瓜出身不能改变起点；自定义出身也不能增加第五个起点。

出身默认流程只影响当前起点之后可以如何承接剧情：

- 来信前起点可以展开不同的认知和家庭反应；
- 对角巷起点可以直接处理陪同者和采购过程；
- 站台起点不得强行重新描写来信或对角巷；
- 分院起点不得强行倒放收信、购物和列车过程。

## 七、测试计划

### 后端内容测试

验证第 6 步：

- 三个预设出身都有独立的一句话页面介绍；
- 自定义出身仍可输入；
- 预设选项数量不增加；
- 起点选项仍然只有四个。

### 后端状态测试

验证：

- 三种预设出身正确生成 `origin_id`；
- 自定义出身生成 `origin_id=custom`；
- 旧中文值、旧英文值和新稳定 ID 都能正确归一化；
- 出身不会修改四种剧情起点的日期、地点和 activity；
- 出身不会修改魔杖或分院里程碑初始逻辑。

### 提示词契约测试

至少覆盖：

- 纯血每轮都有基础介绍词；
- 混血每轮都有基础介绍词；
- 麻瓜每轮都有基础介绍词；
- 自定义出身没有三种预设的基础介绍词；
- 三种预设的分院前背景分别准确注入；
- 自定义出身注入合理推断和默认流程规则；
- `sorting_completed=false` 时存在分院前背景提示词；
- `sorting_completed=true` 时所有分院前背景提示词都移除；
- 基础介绍词在完成分院后仍然存在；
- 玩家行动优先于默认流程；
- 四种剧情起点规则仍存在且不被出身规则替代；
- 魔杖与分院提示词在不同出身下保持独立。

### 前端视觉与交互测试

- 三种预设出身显示各自的一句话介绍；
- 自定义出身输入不会新增提示词选项或第五种剧情起点；
- 四种剧情起点在所有出身下都保持一致；
- 自定义出身测试夹具仍只显示四个起点。

## 八、验收标准

完成后必须满足：

- 预设出身基础介绍词在每轮提示词中持续存在；
- 自定义出身没有预设基础介绍词；
- 三种预设出身在分院前分别拥有不同的默认背景流程；
- 自定义出身由模型根据设定推断，并默认遵循收信到入学的大致流程；
- 默认流程不强制覆盖玩家主动选择；
- 完成分院后，预设和自定义的分院前背景提示词全部移除；
- 基础出身介绍不因分院完成而移除；
- 出身不改变四种剧情起点；
- 出身不改变魔杖和分院提示词的独立判断；
- 后端全量测试、前端相关测试、前端构建和 `git diff --check` 全部通过。

## 九、建议落地顺序

1. 新增统一出身内容源和规范化函数；
2. 调整第 6 步选项与角色状态物化；
3. 拆分并组合提示词层；
4. 增加预设、自定义、分院前后和起点交叉测试；
5. 更新 README 的角色创建说明；
6. 执行后端全量测试、前端测试和构建；
7. 由用户确认后再决定是否提交或推送。
