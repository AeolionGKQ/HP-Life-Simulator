# 霍格沃兹课程与选修系统设计与落地计划

## 1. 文档定位

- 文档状态：设计与落地计划，当前阶段只完成设计，不修改功能代码。
- 适用版本：V1.2 属性、年级与状态系统。
- 目标范围：课程目录、必修课、选修课、N.E.W.T. 课程、课程技能、六月学年结算、九月年级晋升、课程面板和不经过模型的选课流程。
- 不在本阶段处理：
  - 年级状态机中已经存在但尚未修复的其他问题；
  - 学籍剧情证据校验；
  - 日期跨学年跳跃；
  - 被拒绝年级提案的回包一致性；
  - 普通技能之外的完整高级魔法行动裁决。

本系统与年级系统协同，但课程和选修数据必须由程序状态负责。模型可以叙述课堂、提出学习结果和对已有技能提出普通增量，但不能添加、删除或替换玩家课程。

---

## 2. 当前实现审计结论

### 2.1 技能系统现状

当前已经存在技能等级概念，技能状态通常形如：

```json
{
  "skills": {
    "spellcasting": {
      "name": "魔咒",
      "level": 10,
      "experience": 0,
      "source": "initial_magic_talent"
    }
  }
}
```

当前问题：

- `skill_deltas` 的后端范围是 `0—100`；
- `skill_add.level` 没有统一上限；
- 角色创建时的魔法天赋固定写入等级 `10`；
- 没有课程目录，也没有课程与技能之间的稳定关系；
- 没有课程技能的自动获得和六月学年结束自动成长；
- 模型可以通过 `skill_add` 任意创建技能；
- 前端只读展示技能，没有直接选课能力。

### 2.2 学校状态现状

当前 `school` 已有以下预留字段：

```json
{
  "grade": "not_enrolled",
  "enrollment_started": false,
  "owl_results": {},
  "newt_results": {},
  "elective_courses": [],
  "active_courses": []
}
```

当前真正实现的只有：

- 年级变化；
- O.W.L. / N.E.W.T. 结果的基础写入；
- 前端把整个 `school` 对象交给通用信息组件展示。

当前没有：

- 稳定课程目录；
- 必修课自动发放；
- 三年级选修流程；
- 六年级 N.E.W.T. 选课流程；
- 课程资格验证；
- 课程面板专用 API；
- 课程技能自动创建；
- 六月学年结束边界和自动技能成长；
- 选课状态的待处理标记。

### 2.3 回合上下文现状

当前回合已经把以下内容发送给模型：

- 完整 `player_state`；
- `school_rules`；
- `current_skills`；
- `current_statuses`；
- `current_inventory`。

课程字段虽然会随着完整 `player_state.school` 一起发送，但没有专门、稳定、易读的 `current_courses` 上下文。课程系统落地后必须增加专门字段，避免模型只从通用 JSON 中猜测课程状态。

---

## 3. 设计目标与不变量

### 3.1 设计目标

1. 课程是程序权威状态，模型不能修改课程列表。
2. 每门正式课程对应一个稳定的课程技能 ID。
3. 课程技能统一使用 `0—10` 等级。
4. 等级 `0` 表示知道这门课的基本概念，但基本不会实际应用。
5. 课程在学年开始时提供对应技能等级 `0`。
6. 每年首次进入六月时，当前 `active_courses` 对应的课程技能自动增加 `1`，最高为 `10`；该结算只表示当前学年课程完成，不改变角色年级。
7. 学年中模型可以通过普通 `skill_deltas` 提升已有课程技能，但仍受 `0—10` 上限约束。
8. 年级变化时由程序自动发放或触发选课，不调用模型，不创建额外剧情节点。
9. 选课行为必须通过带 `expected_state_version` 的后端 API 直接写入数据库。
10. `year_1—year_6` 只在满足条件的九月新学期开始时逐级晋升；进入一年级仍只由分院完成触发。
11. 六月技能成长与九月年级晋升使用彼此独立的幂等标记，任何一个流程都不能重复触发或代替另一个流程。
12. 下一轮模型请求必须看到最新课程列表、当前学年阶段和课程技能等级。

### 3.2 权威边界

| 内容 | 程序 | 模型 | 玩家前端 |
| --- | --- | --- | --- |
| 课程目录 | 权威维护 | 只读 | 只读 |
| 当前课程列表 | 权威维护 | 只读 | 在合法选课窗口可修改 |
| 选修课程 | 权威维护 | 只读 | 在选课窗口可选择 |
| N.E.W.T. 资格 | 权威计算 | 只能叙述 | 只能从可用列表中选择 |
| 课程技能是否存在 | 权威决定 | 只读 | 不可直接创建 |
| 课程技能等级 | 程序裁决上限与六月自动成长 | 可提出普通增量 | 不直接修改 |
| 六月学年结算 | 程序执行 | 不参与 | 不直接触发 |
| 九月年级晋升 | 程序执行并防重 | 只能按权威状态叙述 | 不直接触发 |
| 课堂剧情 | 不生成叙事 | 负责生成 | 通过行动参与 |
| 选课确认 | 不调用模型 | 不参与 | 直接提交 API |

### 3.3 课程与技能的边界

课程技能属于技能系统的一种受控来源：

```text
课程目录
  -> 课程技能定义
  -> 程序在合法入课时创建技能 level=0
  -> 每年六月学年结束时，仍在 active_courses 的课程技能自动 +1
  -> 学年中普通 skill_deltas 可以继续提升
```

模型不能通过 `skill_add` 创建课程技能。若模型提出的技能 ID 属于课程技能目录但玩家没有正式修读该课程，程序拒绝该提案并记录审计原因。

普通剧情技能仍然可以存在，但所有技能等级统一收敛为 `0—10`。普通技能和课程技能都复用同一个等级裁剪函数。

---

## 4. 课程目录设计

### 4.1 稳定课程 ID

课程 ID 使用稳定英文标识，界面和模型上下文同时提供中文名称：

| 课程 ID | 中文名 | 类别 | 对应技能 ID |
| --- | --- | --- | --- |
| `transfiguration` | 变形术 | core | `transfiguration` |
| `charms` | 咒语 | core | `charms` |
| `potions` | 魔药 | core | `potions` |
| `history_of_magic` | 魔法史 | core | `history_of_magic` |
| `defence_against_dark_arts` | 黑魔法防御术 | core | `defence_against_dark_arts` |
| `astronomy` | 天文学 | core | `astronomy` |
| `herbology` | 草药学 | core | `herbology` |
| `flying` | 飞行课 | first_year_required | `flying` |
| `arithmancy` | 算术占卜 | elective | `arithmancy` |
| `muggle_studies` | 麻瓜研究 | elective | `muggle_studies` |
| `divination` | 占卜 | elective | `divination` |
| `ancient_runes` | 古代魔文研究 | elective | `ancient_runes` |
| `care_of_magical_creatures` | 神奇动物保护 | elective | `care_of_magical_creatures` |
| `alchemy` | 炼金术 | advanced_elective | `alchemy` |
| `apparition` | 幻影移形 | advanced_elective | `apparition` |

`flying` 只在一年级作为正式课程提供。二年级及以后可以出现飞行相关剧情，但不再自动作为常规课程技能成长来源，除非未来增加魁地奇或飞行训练系统。

`alchemy` 和 `apparition` 属于六、七年级可选的专业课程，不属于三年级五门基础选修。

### 4.2 课程目录字段

后端新增统一课程目录，例如 `backend/app/content/courses.py`，每项至少包含：

```python
{
    "id": "charms",
    "name": "咒语",
    "category": "core",
    "skill_id": "charms",
    "available_from_grade": "year_1",
    "available_to_grade": "year_7",
    "third_year_elective": False,
    "newt_eligible": True,
    "owl_required_for_newt": True,
    "teacher_threshold": "E",
    "description": "学习施加、改变和解除魔法效果的系统课程。",
}
```

目录是后端课程规则的唯一来源，并向以下模块提供数据：

- 课程选课校验；
- 自动发放课程；
- 课程技能创建；
- O.W.L. / N.E.W.T. 资格计算；
- 回合提示词上下文；
- 课程 API；
- 前端课程展示；
- 测试夹具。

### 4.3 资料基线与项目规则

依据 Wizarding World 官方课程说明：

- 一年级所有学生学习七门核心课：
  - 变形术；
  - 咒语；
  - 魔药；
  - 魔法史；
  - 黑魔法防御术；
  - 天文学；
  - 草药学。
- 一年级飞行课也是必修。
- 二年级结束时至少从五门选修中选择两门：
  - 算术占卜；
  - 麻瓜研究；
  - 占卜；
  - 古代魔文研究；
  - 神奇动物保护。
- 五年级参加 O.W.L.；
- O.W.L. 通过等级为 `O`、`E`、`A`，不及格为 `P`、`D`、`T`；
- O.W.L. 结果影响六、七年级的 N.E.W.T. 课程；
- 七年级参加 N.E.W.T.；
- 炼金术等高度专业课程只在最后两年且学校有足够需求时开设。

项目 V1 额外采用以下可执行规则：

- 三年级选修最少 2 门，最多 3 门。最多 3 门是游戏中的课业负担限制，不否认原著中存在修读更多课程的特殊可能。
- 三年级选定的选修默认持续到五年级；V1 不开放普通学期中随意换课。
- 六年级 N.E.W.T. 课程最少选择 1 门，最多选择 5 门；若角色选择继续在校但没有任何可用课程，前端必须显示无法继续正常课程的提示。
- N.E.W.T. 课程通常要求对应 O.W.L. 达到 `E` 或 `O`；若课程目录定义了 `A` 可接受，程序按目录执行。
- 教授门槛是课程目录数据，不由模型临时决定。
- 炼金术 V1 默认要求六年级、学校开课、至少两名角色或剧情明确确认课程开设；如没有足够需求，选项不可用。
- 幻影移形视为六年级专业课，必须满足学校开课和年龄/课程规则；课程技能只能从 `0` 开始，不能因为角色自由设定直接获得高等级。

---

## 5. 技能等级系统调整

### 5.1 统一等级范围

所有技能，包括普通技能、天赋转化技能和课程技能，统一使用：

```text
0—10
```

等级含义：

| 等级 | 含义 |
| ---: | --- |
| 0 | 知道基本概念，但基本不会稳定使用 |
| 1 | 能在指导下完成最基础动作 |
| 2 | 能完成简单练习，失败率仍然较高 |
| 3 | 掌握基础流程 |
| 4 | 能独立完成常见低难度任务 |
| 5 | 达到稳定的中等水平 |
| 6 | 能处理较复杂任务 |
| 7 | 高年级熟练水平 |
| 8 | 专业方向的优秀水平 |
| 9 | 接近顶尖，能应对高难度任务 |
| 10 | 当前阶段的极高熟练度，不代表绝对无失败 |

技能等级不等同于行动必然成功。模型仍然必须综合年级、课程、属性、资源、状态、环境和行动难度判断。

### 5.2 现有技能归一化

当前旧逻辑使用 `0—100`，角色创建时魔法天赋写入 `10`。落地时统一改为 `0—10`：

- 初始魔法天赋当前的 `10/100` 等价为新系统的 `1/10`；
- 现有状态中的旧技能等级按 `round(old_level / 10)` 转换，并裁剪到 `0—10`；
- 课程技能不从旧魔法天赋自动推导，正式入课时由课程系统创建为 `0`；
- `experience` 作为当前等级的成长进度，范围为 `0—100`；
- 模型通过 `skill_experience_deltas` 只能为已经存在的技能增加正数经验；
- 经验达到 `100` 时由程序自动提升一级并清零，单次只升一级且不保留溢出经验；
- 已达到 `10` 级的技能不再接受经验增长；
- `skill_experience_deltas` 与原有直接提升等级的 `skill_deltas` 并存，但不能对同一成长重复结算；
- `skill_deltas` 和 `skill_add.level` 都必须经过统一 `clamp_skill_level()`；
- 负数等级、超过 10 的等级、非整数等级必须被裁剪或拒绝并审计。

由于当前 V1.2 的状态版本主要面向新存档，优先保证新存档正确；若检测到旧 `0—100` 技能，应通过状态版本或字段标志只转换一次，避免重复缩放。

### 5.3 课程技能元数据

课程技能应保存来源和课程关系：

```json
{
  "skills": {
    "charms": {
      "id": "charms",
      "name": "咒语",
      "description": "来自咒语课程的技能。",
      "level": 0,
      "experience": 0,
      "source": "course",
      "course_id": "charms",
      "course_skill": true
    }
  }
}
```

普通剧情技能可以继续使用：

```json
{
  "source": "story",
  "course_skill": false
}
```

没有 `course_id` 的历史技能可以被兼容读取，但新写入必须补充 `source`。

---

## 6. 学校课程状态设计

### 6.1 目标状态结构

将现有 `school` 扩展为：

```json
{
  "school": {
    "house": "gryffindor",
    "grade": "year_3",
    "enrollment_started": true,
    "school_year": "1993-1994",
    "grade_started_year": 1993,
    "last_grade_promotion_key": "year_2:year_3:1993",
    "term": "autumn",
    "last_course_progression_year": 1993,
    "departure_reason": null,
    "active_courses": [
      "transfiguration",
      "charms",
      "potions",
      "history_of_magic",
      "defence_against_dark_arts",
      "astronomy",
      "herbology",
      "arithmancy",
      "care_of_magical_creatures"
    ],
    "elective_courses": [
      "arithmancy",
      "care_of_magical_creatures"
    ],
    "newt_courses": [],
    "owl_results": {},
    "newt_results": {},
    "course_selection": null,
    "course_history": []
  }
}
```

字段职责：

- `active_courses`：当前学年正在修读的全部课程，作为模型和课程面板的当前课程来源；
- `elective_courses`：三年级阶段选定、并默认持续到五年级的选修课；
- `newt_courses`：六年级阶段选定的 N.E.W.T. 课程；
- `owl_results`：按课程 ID 保存 O.W.L. 结果；
- `newt_results`：按课程 ID 保存 N.E.W.T. 结果；
- `term`：当前学期，取 `autumn`、`spring`、`summer`；
- `grade_started_year`：当前年级开始时的公历年份；一年级为完成分院时的年份，其他年级为九月晋升时的年份；
- `last_grade_promotion_key`：最近一次九月晋升的唯一键，用于阻止同一新学年重复升年级；
- `last_course_progression_year`：最近一次完成六月课程技能自然成长的公历年份，用于阻止同一年重复成长；
- `course_selection`：待玩家处理的选课窗口；
- `course_history`：记录课程加入、退出、自动发放和选课变更。

### 6.2 六月课程技能年度结算

V1 将每年六月视为当前学年课程结束：

- 进入六月不会改变 `school.grade`；
- 进入六月只结算当前 `active_courses` 对应的课程技能；
- 每门仍在修读的课程技能固定增加 `1`，最高为 `10`；
- 已退课、已结束或不在 `active_courses` 中的课程技能保留原等级，但不参与自然成长；
- 模型仍可在其他时间通过普通 `skill_deltas` 合理提升已有技能。

当程序接受一轮回合日期时：

1. 只有 `current_date.month == 6` 才检查年度课程技能成长；
2. 要求 `last_course_progression_year != current_date.year`；
3. 对结算前 `active_courses` 中每门课程对应的技能自动 `+1`；
4. 结算后立即写入 `last_course_progression_year = current_date.year`；
5. 同一六月后续任何回合都不能再次自然成长；
6. 六月结算不修改 `grade`、`grade_started_year` 或 `school_year`；
7. 自动成长和模型提出的 `skill_deltas` 在同一回合分别审计，并统一裁剪到 `0—10`。

课程技能在退课后保留历史等级，但由于课程已经从 `active_courses` 移除，之后的六月不再自然固定成长。
重新选入时继续使用原技能等级，不重复创建或重置。

### 6.3 九月新学期年级晋升

除一年级外，`year_1—year_6` 的下一年级只在九月新学期开始时改变。程序接受一轮九月日期后，
按以下条件自动检查晋升：

```text
current_grade in year_1..year_6
current_date.month == 9
current_date.year > school.grade_started_year
promotion_key != school.last_grade_promotion_key
```

其中：

```text
promotion_key = "{from_grade}:{to_grade}:{current_date.year}"
```

全部满足时：

1. 只向前晋升一个年级；
2. 设置 `grade_started_year = current_date.year`；
3. 设置 `school_year = "{current_date.year}-{current_date.year + 1}"`；
4. 写入 `last_grade_promotion_key = promotion_key`；
5. 根据新年级刷新 `active_courses`；
6. 如果新年级为三年级或六年级，创建相应 `course_selection` 并阻止下一轮剧情，直到玩家完成选课；
7. 同一九月后续回合因为 `grade_started_year` 和 `last_grade_promotion_key` 已更新，不得再次晋升。

`not_enrolled -> year_1` 不使用九月自动晋升规则，仍然只在分院剧情完成并被程序接受后发生。
六月课程技能结算也不能触发年级变化。

回合协议中的普通晋升原因同步改为 `new_school_year_started`；旧的 `summer_departure` 不再表示年级变化。
模型可以返回九月开学叙事和晋升信息，但最终是否晋升、晋升到哪一级以及是否已经触发过，始终由程序状态决定。

---

## 7. 自动课程发放与选课状态机

### 7.1 入学前

`grade = not_enrolled` 时：

- `active_courses = []`；
- 不创建霍格沃茨课程技能；
- 家庭教育、童年失控魔法和非正式基础常识不作为正式课程技能；
- 课程面板显示“尚未入学，正式课程将在分院后建立”。

### 7.2 进入一年级

当程序接受 `not_enrolled -> year_1`：

1. 自动加入七门核心课；
2. 自动加入一年级飞行课；
3. 为上述八门课程创建对应技能，等级均为 `0`；
4. 设置 `grade_started_year = current_date.year`；
5. 设置 `term` 为当前日期对应的第一学期；
6. 设置 `course_selection = null`；
7. 写入 `course_history`，记录 `reason = enrollment`；
8. 不调用模型，不生成额外剧情节点。

目标课程：

```text
transfiguration
charms
potions
history_of_magic
defence_against_dark_arts
astronomy
herbology
flying
```

### 7.3 二年级

进入二年级时：

- `active_courses` 变为七门核心课；
- 飞行课不再作为自动正式课程；
- 已有飞行技能保留，但因为飞行课不再位于 `active_courses`，之后六月不再自然固定提升；
- 选修列表仍为空；
- 不允许通过课程面板提前选择三年级选修。

### 7.4 首次进入三年级

当程序在九月首次自动完成 `year_2 -> year_3`：

1. 先设置年级为 `year_3`；
2. 不直接调用模型；
3. 创建待选课状态：

```json
{
  "course_selection": {
    "phase": "elective",
    "trigger_grade": "year_3",
    "min_select": 2,
    "max_select": 3,
    "options": [
      "arithmancy",
      "muggle_studies",
      "divination",
      "ancient_runes",
      "care_of_magical_creatures"
    ],
    "status": "pending"
  }
}
```

4. 前端立即弹出选修课选择框；
5. 玩家提交后：
   - 写入 `elective_courses`；
   - 把核心课与选修课写入 `active_courses`；
   - 为新增选修课创建技能等级 `0`；
   - 清除 `course_selection`；
   - 增加 `state_version`；
   - 写入 `course_history`；
6. 选课请求不产生 `TurnRecord`、纪事或剧情节点；
7. 在选课完成前，禁止继续发送下一轮模型请求。

### 7.5 三至五年级

三至五年级：

- `active_courses = 七门核心课 + elective_courses`；
- 选修课默认持续到五年级；
- V1 不允许普通学期随意换选修；
- 如未来增加退选功能，必须通过同一课程 API，并记录退选原因；
- 五年级所有当前课程都可以生成 O.W.L. 考试成绩；
- O.W.L. 结果只能写入当前实际修读课程。

### 7.6 首次进入六年级

当程序在九月首次自动完成 `year_5 -> year_6`：

1. 必须已经完成 O.W.L.；
2. 根据 `owl_results`、课程目录中的 `teacher_threshold` 和学校开课状态生成可选 N.E.W.T. 课程；
3. 创建待选课状态：

```json
{
  "course_selection": {
    "phase": "newt",
    "trigger_grade": "year_6",
    "min_select": 1,
    "max_select": 5,
    "options": [
      {
        "course_id": "charms",
        "available": true,
        "reason": "O.W.L. 达到 E，满足咒语教授门槛"
      },
      {
        "course_id": "potions",
        "available": false,
        "reason": "O.W.L. 成绩未达到该课程门槛"
      }
    ],
    "status": "pending"
  }
}
```

4. 前端弹出 N.E.W.T. 选课框；
5. 玩家只能从 `available=true` 的课程中选择；
6. 提交后：
   - 写入 `newt_courses`；
   - `active_courses = newt_courses`；
   - 为没有技能记录的课程创建等级 `0`；
   - 已有课程技能保留原等级；
   - 清除 `course_selection`；
   - 增加 `state_version`；
   - 写入 `course_history`；
7. 选课完成前禁止继续剧情请求。

六、七年级不能自动把所有核心课程继续保留为 N.E.W.T. 课程，必须以玩家选择和 O.W.L. 资格为准。

### 7.7 已离校

`grade = left_school` 时：

- `active_courses = []`；
- 保留 `course_history`、`owl_results`、`newt_results` 和历史课程技能；
- 课程面板改为只读；
- 不再触发六月课程技能自然成长；
- `departure_reason` 正常显示。

---

## 8. 不经过模型的课程操作

### 8.1 API 设计

新增课程查询接口：

```http
GET /api/sessions/{session_id}/courses
```

返回：

```json
{
  "session_id": "session-id",
  "state_version": 12,
  "grade": "year_3",
  "active_courses": [],
  "elective_courses": [],
  "newt_courses": [],
  "course_skill_levels": {},
  "selection": {
    "phase": "elective",
    "min_select": 2,
    "max_select": 3,
    "options": []
  }
}
```

新增直接选课接口：

```http
PUT /api/sessions/{session_id}/courses
```

请求：

```json
{
  "expected_state_version": 12,
  "selection_phase": "elective",
  "course_ids": [
    "arithmancy",
    "care_of_magical_creatures"
  ]
}
```

后端必须：

- 校验存档存在；
- 校验当前状态版本；
- 校验角色未死亡、未离校；
- 校验存在对应的 `course_selection.status = pending`；
- 校验选课阶段与请求阶段一致；
- 校验课程 ID 来自目录；
- 校验数量满足最小和最大值；
- 校验课程当前可用；
- 校验没有重复课程；
- 校验三年级选修和六年级 N.E.W.T. 资格；
- 在一个数据库事务中写入课程、技能、历史和状态版本；
- 返回新的课程视图与新的 `state_version`。

版本冲突返回 `409`，前端重新读取课程状态后提示玩家确认最新选择。

### 8.2 模型权限限制

回合提示词新增明确规则：

```text
课程列表、选修课程、N.E.W.T. 课程和 course_selection 只能由程序与玩家课程 API 修改。
模型不得通过 player_changes、state_proposals、events 或叙事文字添加、删除、替换课程。
模型只能对当前已存在的课程技能提出普通 skill_deltas。
模型不得使用 skill_add 创建课程技能；课程技能由程序在合法入课时创建。
```

后端不能只依赖提示词，必须在状态裁决层：

- 拒绝模型提出的课程字段；
- 拒绝 `skill_add` 创建课程技能；
- 拒绝对未正式修读课程技能的 `skill_deltas`；
- 允许对已有课程技能提出普通增量；
- 统一将技能等级限制在 `0—10`。

### 8.3 课程面板交互

课程面板分为三部分：

1. 当前课程：
   - 当前年级；
   - 当前学年；
   - 当前学期；
   - 正在修读的课程；
   - 每门课程技能等级；
   - 下一次六月年度结算时间；
   - 当前年级开始年份和下一次九月晋升条件。
2. 课程成绩：
   - O.W.L. 成绩；
   - N.E.W.T. 成绩；
   - 每门课程是否具备继续学习资格。
3. 课程操作：
   - 存在三年级选课窗口时显示三年级选修选择框；
   - 存在六年级选课窗口时显示 N.E.W.T. 选择框；
   - 非选课窗口显示只读状态；
   - 已离校后完全只读。

选课弹窗必须是独立 UI 状态，不放入剧情节点历史，不改变故事导航索引。

---

## 9. 回合上下文与提示词

每轮发送给模型的上下文新增：

```json
{
  "school": {
    "grade": "year_3",
    "school_year": "1993-1994",
    "term": "autumn",
    "active_courses": [
      {
        "course_id": "charms",
        "name": "咒语",
        "category": "core",
        "skill_id": "charms",
        "skill_level": 2,
        "owl_grade": null,
        "newt_grade": null
      }
    ],
    "elective_courses": [
      "arithmancy",
      "care_of_magical_creatures"
    ],
    "newt_courses": [],
    "course_selection_pending": false
  },
  "current_courses": [
    {
      "course_id": "charms",
      "name": "咒语",
      "skill_id": "charms",
      "skill_level": 2
    }
  ],
  "course_rules": {
    "course_state_is_program_authoritative": true,
    "model_can_modify_courses": false,
    "skill_level_range": [0, 10]
  }
}
```

模型必须知道：

- 当前正在上的课程；
- 每门课程对应的技能和技能等级；
- 当前学期；
- O.W.L. / N.E.W.T. 成绩；
- 当前是否存在待选课窗口；
- 课程列表不能被模型修改。

如果本轮日期首次进入六月，程序负责执行本学年的课程技能结算，模型不需要返回课程年度增长字段。
如果本轮日期满足九月晋升条件，程序负责权威年级晋升；模型不能让六月结算提前改变年级，也不能在同一九月重复晋升。

---

## 10. 六月课程技能成长规则

### 10.1 自动成长

每年首次进入六月时：

```text
active_course.course_skill.level += 1
```

自动成长只作用于当时仍在 `active_courses` 中的课程技能，最高为 `10`。课程已经退选或结束时，
技能条目仍然保留，但不再自然固定成长。

审计示例：

```json
{
  "course_skill_progression": [
    {
      "course_id": "charms",
      "skill_id": "charms",
      "before": 2,
      "after": 3,
      "delta": 1,
      "reason": "academic_year_completed",
      "progression_year": 1994
    }
  ],
  "last_course_progression_year": 1994
}
```

防重条件：

```text
current_date.month == 6
last_course_progression_year != current_date.year
```

结算成功后必须先持久化 `last_course_progression_year`，再返回响应。相同日期重试、同一六月多轮对话或
服务重启后继续游玩，都不得重复增加技能等级。

### 10.2 模型普通提升

模型仍可以使用现有 `skill_deltas`：

```json
{
  "skill_deltas": {
    "charms": 1
  }
}
```

程序验证：

- 技能必须已存在；
- 课程技能必须对应当前或历史合法课程；
- 单回合增量遵守技能变化幅度限制；
- 最终等级不超过 `10`；
- 不能通过 `skill_deltas` 添加不存在的课程技能；
- 六月自动增长和模型增长分别记录。

### 10.3 等级与课程表现

技能等级只表示能力熟练度，不直接等价于考试成绩。O.W.L. 和 N.E.W.T. 仍由模型根据课程技能、长期维度、学习经历、考试行动和状态提出结果，再由程序校验课程资格与成绩格式。

---

## 11. 状态协议调整

### 11.1 PlayerChanges

本阶段不允许模型修改课程，因此 `PlayerChanges` 不新增课程字段。

课程直接操作使用独立的 `CourseSelectionRequest`：

```python
class CourseSelectionRequest(BaseModel):
    expected_state_version: int = Field(ge=0)
    selection_phase: Literal["elective", "newt"]
    course_ids: list[str] = Field(min_length=1, max_length=5)
```

这样可以明确区分：

- 模型回合中的叙事与状态提案；
- 玩家直接执行的课程选择命令。

### 11.2 课程响应模型

新增：

- `CourseOptionRead`；
- `CourseSkillRead`；
- `CourseSelectionRead`；
- `CourseView`。

响应必须提供中文名称、稳定 ID、可用性、不可用原因、技能等级和考试成绩，避免前端猜测后端规则。

### 11.3 课程技能协议

统一规定：

```python
SKILL_LEVEL_MIN = 0
SKILL_LEVEL_MAX = 10
```

所有技能写入路径统一使用：

```python
clamp_skill_level(value)
```

需要覆盖：

- 角色创建初始魔法天赋；
- `skill_deltas`；
- `skill_add`；
- 自动课程技能创建；
- 六月课程技能年度成长；
- 课程 API 选课后的技能创建。

---

## 12. 分阶段落地计划

### 阶段一：课程目录与技能等级基线

目标：建立所有后续模块共享的规则源。

工作：

- 新增 `backend/app/content/courses.py`；
- 定义课程 ID、中文名、类别、技能 ID、年级范围和考试门槛；
- 定义 `CORE_COURSES`、`FIRST_YEAR_COURSES`、`THIRD_YEAR_ELECTIVES`；
- 定义 O.W.L. 通过等级和教授门槛；
- 新增统一 `clamp_skill_level()`；
- 把技能范围从 `0—100` 改为 `0—10`；
- 将初始魔法天赋从旧的等级 10 归一为等级 1；
- 为 `skill_add` 增加等级校验。

验证：

- 所有技能写入路径都不超过 10；
- 负数和超范围值被裁剪或拒绝；
- 课程目录无重复技能 ID；
- 不同模块使用同一课程目录。

### 阶段二：学校课程状态与自动发放

目标：让年级变化能够自动产生课程和课程技能。

工作：

- 扩展 `school` 状态；
- 入学进入一年级时自动加入七门核心课和飞行课；
- 二年级移除飞行课的正式自动课程身份；
- 三至五年级使用核心课加已选选修；
- 创建课程技能等级 `0`；
- 保留课程历史和课程技能来源；
- 增加课程自动发放测试。

验证：

- 未入学无正式课程技能；
- 一年级自动获得八门课程；
- 二年级只能修七门核心课；
- 课程技能来源、课程 ID 和等级正确。

### 阶段三：六月结算、九月晋升与技能成长

目标：落实每年六月当前课程技能固定提升一级，并将二至七年级晋升统一放在九月新学期。

工作：

- 实现六月年度结算识别；
- 使用 `last_course_progression_year` 防止同一六月重复成长；
- 使用 `grade_started_year` 和 `last_grade_promotion_key` 防止同一九月重复晋升；
- 六月只提升 `active_courses` 的课程技能，不改变年级；
- 九月只逐级改变年级，不重复执行六月技能增长；
- 自动成长限制在 `0—10`；
- 与模型 `skill_deltas` 合并并分别审计；
- 处理退课后课程技能保留但停止自然成长。

验证：

- 每个公历年的六月最多结算一次；
- 同一六月多轮对话不会重复结算；
- 六月结算后年级保持不变；
- 非九月不能从一年级升到二年级；
- 九月满足年份条件时只晋升一级；
- 同一九月多轮对话不会重复升年级；
- 课程技能最高为 10；
- 模型提升和自动提升都能被追溯；
- 已退选课程技能不再自动增长。

### 阶段四：三年级选修流程

目标：在首次进入三年级时强制弹出不经过模型的选课流程。

工作：

- 年级转换后创建 `course_selection.phase = elective`；
- 新增课程查询 API；
- 新增课程提交 API；
- 实现最少 2 门、最多 3 门校验；
- 选课成功后写入课程、技能和历史；
- 前端显示独立选课弹窗；
- 选课完成前阻止下一轮剧情。

验证：

- 三年级只触发一次；
- 不能少选或超选；
- 不能选择非法 ID；
- 选课不生成 TurnRecord；
- 选课后下一轮模型能看到最新课程；
- 并发版本冲突返回 409。

### 阶段五：O.W.L. 与六年级 N.E.W.T. 选课

目标：让六年级选课真正由 O.W.L. 成绩和课程目录裁决。

工作：

- 限制 O.W.L. 只能发生在五年级当前修读课程；
- 校验每门成绩的合法性；
- 持久化 `owl_results`；
- 根据每门课程的 O.W.L. 和教授门槛生成可用列表；
- 支持炼金术等最后两年专业课程；
- 首次进入六年级创建 N.E.W.T. 待选课；
- 写入 `newt_courses` 和 `active_courses`；
- 六、七年级只传 N.E.W.T. 当前课程给模型。

验证：

- 不同 O.W.L. 成绩导致不同可选列表；
- 未达门槛课程不可选；
- 六年级选课只能提交可用课程；
- 课程数量符合最小/最大规则；
- 六年级选课不调用模型；
- N.E.W.T. 结果只能发生在七年级当前课程。

### 阶段六：课程面板与前端交互

目标：让玩家能查看当前课程并在合法窗口直接修改。

工作：

- 新增前端课程 API 类型；
- 课程页改为专用渲染，不再直接显示原始 `school` JSON；
- 显示当前年级、学年、学期、课程、技能等级和考试成绩；
- 显示不可用课程及原因；
- 实现三年级和六年级选课弹窗；
- 处理版本冲突、重复提交和加载状态；
- 移动端检查无横向溢出。

验证：

- 课程名称全部中文可读；
- 技能等级显示为 `0/10` 至 `10/10`；
- 选课窗口不可绕过；
- 课程面板操作不会创建剧情节点；
- 选课期间剧情按钮被锁定；
- 桌面和移动端均通过视觉测试。

### 阶段七：提示词、回归测试与文档

目标：完成模型权限边界和项目验收。

工作：

- 更新回合协议说明；
- 增加当前课程专用上下文；
- 明确模型不得修改课程；
- 明确模型只能对已有技能使用普通增量；
- 增加课程、技能和选课测试；
- 更新 V1.2 设计文档和 README；
- 运行完整后端、前端构建和 Playwright 测试。

---

## 13. 预计涉及文件

### 新增或重点修改后端

- `backend/app/content/courses.py`
- `backend/app/content/skills.py`（如果统一技能目录单独拆分）
- `backend/app/schemas/game.py`
- `backend/app/schemas/courses.py`（如课程响应模型独立）
- `backend/app/services/courses.py`
- `backend/app/services/sessions.py`
- `backend/app/services/setup.py`
- `backend/app/services/turns.py`
- `backend/app/rules/state.py`
- `backend/app/prompts/turn.py`
- `backend/app/api/routes.py`

### 重点修改前端

- `frontend/src/api.ts`
- `frontend/src/GameView.tsx`
- `frontend/src/App.tsx`（如需全局弹窗或剧情锁定）
- `frontend/src/styles.css`

### 测试

- `backend/tests/test_attributes.py`
- `backend/tests/test_sessions.py`
- `backend/tests/test_turn_output_contract.py`
- 新增 `backend/tests/test_courses.py`
- `frontend/tests/visual-review.spec.ts`

### 文档

- `霍格沃兹人生模拟器_V1.2属性与维度系统设计.md`
- `README.md`

---

## 14. 测试与验收标准

### 14.1 技能等级

- 课程技能创建时等级为 0；
- 所有技能等级最终都在 0—10；
- 旧 0—100 技能只归一化一次；
- 初始魔法天赋按新规则写入；
- 模型不能通过 `skill_add` 创建课程技能；
- 模型不能让未修读课程技能凭空出现；
- 模型可以提升已存在课程技能，但不能超过 10。

### 14.2 课程自动发放

- 未入学没有正式课程；
- 一年级自动获得七门核心课和飞行课；
- 二年级只能修七门核心课；
- 三年级选课前不能继续剧情；
- 三年级选课后自动获得对应课程和技能；
- 六年级选课根据 O.W.L. 结果生成；
- 已离校课程面板只读。

### 14.3 六月成长与九月晋升

- 每个公历年的六月最多结算一次；
- 六月首次结算时，当前 `active_courses` 对应技能统一 `+1`；
- 同一六月重复请求不会重复增长；
- 六月结算不改变角色年级；
- 二至七年级只在九月满足年份条件时逐级晋升；
- 同一九月重复请求不会重复晋升；
- 一年级仍然只由分院完成触发；
- 自动增长和模型增长都能单独审计；
- 课程技能不会超过 10；
- 退课课程技能保留，但不再自然固定成长，也不再作为当前课程参与课堂和新资格判断。

### 14.4 选课 API

- 课程 API 不触发模型；
- 课程 API 不生成剧情节点；
- 课程 API 受状态版本保护；
- 过期版本返回 409；
- 非法课程 ID 被拒绝；
- 数量不足或过多被拒绝；
- 不满足 O.W.L. 门槛被拒绝；
- 选课提交具有幂等性。

### 14.5 模型上下文

- 每轮包含当前年级、学年和学期；
- 每轮包含当前正在修读的课程；
- 每门当前课程包含技能 ID和等级；
- 每轮包含 O.W.L. / N.E.W.T. 成绩；
- 模型明确知道课程由程序和玩家 API 管理；
- 模型不能通过状态提案修改课程。

### 14.6 前端

- 课程面板显示当前课程；
- 课程技能显示中文名和 `0/10` 至 `10/10`；
- 三年级选课弹窗可用；
- 六年级选课弹窗根据成绩禁用不合格课程；
- 选课期间剧情入口锁定；
- 选课后无需刷新浏览器即可看到新课程；
- 桌面和移动端无横向溢出。

---

## 15. 落地顺序与暂停点

本设计建议按以下顺序实施：

```text
课程目录与技能等级
  -> 学校课程状态
  -> 自动课程发放
  -> 六月技能成长与九月年级晋升
  -> 三年级选修 API 与弹窗
  -> O.W.L. 资格与六年级选课
  -> 课程面板
  -> 模型上下文与完整测试
```

每个阶段完成后暂停并运行对应测试。只有阶段一至阶段七全部通过，才将课程系统章节正式合并进 V1.2 设计基准文档，并开始处理其他年级审查问题。

当前计划的第一步是阶段一：先统一技能等级为 `0—10`，建立课程目录和课程技能元数据；在这一步完成前，不实现前端选课弹窗，避免前端提前绑定不稳定的数据结构。

---

## 16. 资料来源

- Wizarding World / Harry Potter Digital：
  - [Hogwarts School Subjects](https://www.harrypotter.com/writing-by-jk-rowling/hogwarts-school-subjects)
  - [Harry Potter 101: The subjects](https://www.harrypotter.com/features/harry-potter-101-the-subjects)
- Harry Potter Lexicon：
  - [Hogwarts Classes](https://www.hp-lexicon.org/thing/hogwarts-classes)
  - [N.E.W.T.s](https://www.hp-lexicon.org/thing/n-e-w-t-s)
- Harry Potter Wiki：
  - [Hogwarts subjects](https://harrypotter.fandom.com/wiki/Hogwarts_subjects)
  - [Third year](https://harrypotter.fandom.com/wiki/Third_year)
  - [Sixth year](https://harrypotter.fandom.com/wiki/Sixth_year)

资料中对三年级选修的最低数量和 O.W.L. 对 N.E.W.T. 的影响较为一致；具体教授门槛、课程最大数量和炼金术开课需求在社区资料中存在差异，因此本计划将其配置化，而不是写死在模型提示词中。
