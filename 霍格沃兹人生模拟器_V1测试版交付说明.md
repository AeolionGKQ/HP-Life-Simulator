# 霍格沃兹人生模拟器 V1 测试版交付说明

## 当前可玩闭环

V1 测试版已经可以完成以下流程：

1. 启动本地 FastAPI 服务和 React 页面。
2. 创建独立本地存档。
3. 完成 13 轮角色创建。
4. 生成玩家完整初始状态、子世代 NPC 和玩家-NPC 关系。
5. 确认角色后进入剧情页面。
6. 调用配置中的 OpenAI-compatible LLM 生成第一幕剧情。
7. 显示正文、普通选项、“其他”自由输入和世界线偏移率。
8. 提交选择并推进下一回合。
9. 保存时间、地点、属性、技能、关系、物品、世界线、回合和纪事。
10. 刷新页面后恢复最新剧情、状态、选项和查询数据。
11. 通过前端菜单查看角色、纪事、恋爱、好感、关系、声望、课程、信件和世界线。

## 运行

项目根目录执行：

```powershell
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

另开终端：

```powershell
cd frontend
npm run dev
```

访问 `http://127.0.0.1:5173`。

如果已经构建前端，也可以让 FastAPI 直接提供 `frontend/dist`：

```powershell
cd frontend
npm run build
cd ..
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

## 配置

运行时配置在：

```text
config/settings.local.toml
```

其中的 API Key 不进入代码、前端资源或接口响应。该文件已加入 `.gitignore`。

## 已实现的长期记忆

- 最近 10 个叙事回合原文窗口。
- token 估算上限。
- 重要事件结构化记忆。
- 事件原文通过 `source_turn_ids` 关联回合。
- SQLite 标签/关键词自动召回。
- LLM `memory_request` 一次内部补查。
- 补查不推进时间、不写入纪事、不修改状态。

## 已实现的规则裁决

- 时间按回合推进。
- 地点更新。
- HP、MP、SP、属性、技能、声望和物品变化的基础裁决。
- 好感度和信任度范围裁剪。
- 玩家年龄与恋爱阶段的基础年龄限制。
- 未满足年龄时记录待解锁关系阶段。
- 世界线偏移率限制在 0–100。
- 回合版本检查。
- 存档内动作幂等。
- 模型服务错误不会写入半个回合。

## 玩家状态变更和词条

每个正式剧情回合的 LLM 响应使用 `player_changes` 字段声明玩家状态变化：

- `inventory_add` / `inventory_remove`：获得或失去物品。
- `status_add` / `status_remove`：获得或解除状态。
- `skill_add` / `skill_remove` / `skill_deltas`：获得、失去或提升技能。
- `trait_add` / `trait_remove`：获得或失去正面、负面词条。
- `vital_deltas`、`attribute_deltas`、`reputation_deltas`：生命、属性和声望变化。

每个可能造成变化的选项使用 `effects.gains` 和 `effects.losses` 提前说明后果。正式回合返回后，后端会校验并写入玩家状态，再通过 `applied_changes` 返回实际生效的变化。前端会在剧情正文下显示“本回合状态变化”。

词条包含：

- 稳定 ID。
- 名称。
- 正面/负面极性。
- 简短作用描述。
- 获得来源和原因。

词条会保存在玩家状态中，并在每轮 Prompt 中作为 `current_traits` 和完整玩家状态传给 LLM。后端每回合最多实际新增两个词条，普通对话不会自动生成词条；新增词条缺少作用描述时不会写入状态。

## 验证结果

```text
python -m pytest
5 passed

npm run build
通过

真实配置模型第一回合
成功，正文、3 个选项和世界线字段已写入本地存档
```

## V1 测试版的内容边界

当前版本重点验证系统闭环，内容仍是第一批可玩内容：

- 子世代主要 NPC 已建立基础档案。
- 13 轮创建选项已经可用，但部分自定义字段仍以文本形式保存。
- 课程、信件、声望、收藏和恋爱菜单已经接入状态展示，完整内容会继续填充。
- 战斗、死亡、完整原著节点分支、复杂 NPC 自治和学年内容还需要继续扩展。
- 当前剧情回合使用普通 HTTP 请求，流式 SSE 作为下一步体验优化。

这版的核心验收目标是：本地单人环境下，玩家可以从零创建角色，真实调用模型，连续推进剧情，查看并恢复存档，且状态不会因模型输出或页面刷新丢失。

