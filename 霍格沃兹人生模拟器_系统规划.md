# 霍格沃兹人生模拟器：系统规划

版本：v0.1  
规划依据：`霍格沃兹人生模拟器.docx`  
定位：基于 LLM 的状态型、分支型、可存档网页人生模拟器

## 1. 项目目标与设计结论

### 1.1 产品目标

玩家创建一名属于自己的巫师角色，选择时代、身份、背景和人生起点，在遵循魔法世界基本规则的前提下，通过对话和选项推进人生。LLM 负责担任“主持人”，描述世界、扮演 NPC、生成遭遇和给出选择；游戏程序负责保存事实状态、执行规则、校验模型输出并管理存档。

核心体验不是“让模型自由写小说”，而是：

- 玩家拥有明确的决定权，模型不能替玩家选择。
- 世界按照时间、地点、事件和 NPC 日程持续运行。
- 每次选择都会产生可追溯的后果。
- 关键数值和关系会长期保持一致。
- 原著时间线可以被玩家改变，但改变必须留下世界线和因果记录。
- 一次人生可以死亡、结束或进入结局；玩家可以通过多个独立存档体验不同人生。

### 1.2 推荐技术路线

初步规划的 React + Python 方向是合适的，建议具体化为：

- 前端：React + TypeScript + Vite。使用 TanStack Query 管理服务端状态，使用 Zustand 或 React Context 管理当前会话 UI 状态。
- 后端：Python + FastAPI。适合异步调用 LLM、SSE 流式输出、结构化数据校验和后续拆分服务。
- 数据库：开发阶段 SQLite，正式部署切换 PostgreSQL。使用 SQLAlchemy 2.x + Alembic。
- 实时交互：首选 SSE。剧情生成是服务器单向流式返回，玩家提交选择仍使用普通 HTTP；只有需要服务器主动推送事件时再引入 WebSocket。
- LLM 接口：自建 OpenAI-compatible provider adapter，支持 `base_url`、API key、模型名、超时、重试和结构化输出能力配置。
- 结构化协议：JSON Schema/Pydantic 模型。模型输出不能直接作为数据库更新指令执行。
- 部署：单体 FastAPI + React 静态资源即可启动 MVP；后续再拆分任务队列、向量检索和多用户服务。

### 1.3 最重要的架构边界

程序是“事实裁判”，LLM 是“叙事主持人”。

程序负责：

- 当前日期、时间、地点和学年。
- 玩家 HP、MP、SP、精力、饱食度、技能、物品和金钱。
- NPC 基础档案、关系数值、关系阶段和已知秘密。
- 世界线变动率、原著节点状态和分支因果。
- 课程、考试、事件触发条件、死亡判定和存档版本。
- 对模型返回内容进行格式和规则校验。

LLM 负责：

- 将当前状态转化为自然、沉浸的叙事。
- 扮演 NPC 并生成符合档案的对话。
- 在允许的事件范围内设计遭遇、冲突和选项。
- 对玩家的自由文本行为进行意图识别和叙事化处理。
- 生成“建议的状态变化”和“下一步选项”，但不能绕过程序裁决。

## 2. 设定文档的系统化拆分

原文可以作为产品设定总纲，但运行时不应每轮把整份文档塞进提示词。建议拆成以下内容：

### 2.1 不可变世界规则

包括七条顶层法则、魔法体系、甘普基本变形法则、不可饶恕咒后果、时代边界、学院基础资料和原著关键事实。

这些内容属于版本化的 `world bible`，由开发者维护。每个游戏会话引用一个世界规则版本，避免规则在中途变化。

### 2.2 时代包

四个时代分别作为独立配置：

- 邓布利多时代：1892–1899。
- 亲世代：1971–1981及其战争阶段。
- 子世代：1991–1998。
- 现代：2020 年以后。

每个时代包应包含：

- 可用出生年份和玩家年龄范围。
- 初始地点、学校状态、教授阵容和 NPC 可用范围。
- 原著事件时间表。
- 可改写节点及其允许的改写方式。
- 时代专属物品、课程、职业和社会背景。
- 该时代允许生成的新 NPC 范围。

### 2.3 内容资料库

NPC、地点、课程、技能、咒语、物品、事件、CG、成就和职业都应从长文档中拆成独立记录，带有稳定 ID、标签、时代范围和解锁条件。

建议使用“结构化资料 + 可检索文本”的双层形式：

- 结构化字段用于规则判定和面板展示。
- 描述文本用于提示词和叙事风格。
- 关系、事件和玩家经历只保存在具体存档中，不写回全局资料。

### 2.4 玩家初始设定流程

初始化严格按文档的 13 轮执行：

1. 时代。
2. 姓名、性别、生日、性取向。
3. 外貌与体格。
4. 家族与血统。
5. 童年经历，选择三项。
6. 性格倾向。
7. 信仰与价值观。
8. 魔杖。
9. 初始天赋专精。
10. 初始宠物。
11. 初始好友。
12. 初始剧情起点。
13. 最终确认、修改或重置。

这 13 轮不应完全依赖通用 LLM。后端应根据当前轮次生成合法选项、校验互斥条件和保存草稿；LLM 只负责呈现叙事和解释选项差异。

## 3. 总体架构

```text
┌──────────────────────┐
│ React Web Client      │
│ 剧情 / 选项 / 角色 / 纪事 │
└──────────┬───────────┘
           │ HTTP + SSE
┌──────────▼───────────┐
│ FastAPI 应用层         │
│ 会话、命令、回合、鉴权  │
└──────┬─────────┬─────┘
       │         │
┌──────▼─────┐ ┌─▼────────────────┐
│ 状态与规则引擎 │ │ LLM Orchestrator │
│ 时间/关系/战斗 │ │ Prompt/Provider  │
└──────┬─────┘ └─┬────────────────┘
       │         │
┌──────▼─────────▼─────┐
│ PostgreSQL / SQLite   │
│ 存档、事件、快照、配置 │
└──────────────────────┘
```

### 3.1 主要后端模块

- `session`：创建、列出、加载、复制、删除和结束存档。
- `character`：玩家角色与初始设定流程。
- `turn`：接收玩家行动、构造回合上下文、调用模型、提交结果。
- `rules`：时间、属性、技能、物品、关系、声望、世界线和死亡规则。
- `timeline`：时代原著节点、预定事件、分支事件和触发窗口。
- `npc`：NPC 档案、日程、关系网络和自治事件。
- `command`：`/状态`、`/时间`、`/关系` 等只读指令以及受控的调试指令。
- `prompt`：上下文分层、摘要、检索和模型消息编排。
- `provider`：OpenAI-compatible 服务适配、重试、超时和模型能力检测。
- `validator`：Pydantic/JSON Schema 校验、状态变化合法性检查和失败重试。
- `content`：世界观、时代、NPC、事件和选项配置加载。

## 4. 回合推进流程

每个玩家动作都形成一个不可变回合记录，并通过事务提交状态。

### 4.1 标准流程

1. 前端提交 `session_id`、当前回合版本、动作类型和动作内容。
2. 后端检查存档是否存在、是否已结束、版本是否匹配，防止重复提交和旧页面覆盖新状态。
3. 读取当前快照和最近事件。
4. 执行时间和可行动性预检查，例如死亡、昏迷、课程冲突、地点限制或道具缺失。
5. 组装提示词：世界规则、时代事实、玩家状态、相关 NPC、近期纪事、当前事件和本次行动。
6. 调用 LLM，要求返回严格结构化的主持人回合。
7. 后端验证 JSON 格式、选项数量、选项 ID、禁止事项和模型声明的状态变化。
8. 规则引擎重新计算时间、数值变化、关系变化、事件解锁和死亡判定。
9. 若模型结果与规则裁决冲突，优先采用程序裁决，并要求模型根据裁决重新生成叙事；必要时返回安全降级文本。
10. 写入回合、玩家动作、叙事、选项、事件差异和状态快照。
11. 通过 SSE 将叙事和结构化选项返回前端。
12. 前端渲染剧情，等待玩家下一次行动。

### 4.2 回合的幂等性

每个请求带有 `client_action_id` 和 `expected_state_version`：

- 同一 `client_action_id` 重复提交时返回原结果。
- 版本不一致时拒绝写入并要求前端刷新。
- LLM 超时不会产生半个回合。
- 数据库提交成功前，不把模型文本展示为已落盘事实。

## 5. LLM 输出协议

模型不得直接返回一段无法解析的自由文本。建议每轮返回类似以下结构：

```json
{
  "turn": {
    "title": "雨夜的禁林边缘",
    "scene_type": "encounter",
    "narrative": "……",
    "time_advance_minutes": 20,
    "location_id": "forbidden_forest_edge",
    "visible_facts": [
      "玩家听见树丛深处传来金属碰撞声"
    ]
  },
  "choices": [
    {
      "id": "choice_1",
      "label": "举起魔杖，先观察声音的来源",
      "kind": "action",
      "risk": "medium",
      "requires": [],
      "effects_hint": "可能进入冲突或发现线索"
    },
    {
      "id": "choice_2",
      "label": "立刻返回城堡并通知教授",
      "kind": "action",
      "risk": "low",
      "requires": [],
      "effects_hint": "可能错过现场，但降低即时危险"
    },
    {
      "id": "choice_other",
      "label": "其他",
      "kind": "free_text",
      "risk": "unknown",
      "requires": []
    }
  ],
  "proposed_changes": {
    "unlocked_events": [],
    "relationship_deltas": [],
    "item_deltas": [],
    "skill_deltas": []
  },
  "host_checks": {
    "canonical_facts_used": ["timeline_event_x"],
    "worldline_change_reason": null,
    "player_decision_preserved": true
  }
}
```

说明：

- `proposed_changes` 只是模型建议，不是最终状态。
- `time_advance_minutes` 由规则引擎根据动作类型、事件类型和上下文复核。
- 每轮必须保留最后一个 `free_text` 类型的“其他”选项。
- 选择项必须有稳定 ID，不能只依赖显示文本。
- `risk` 用于界面提示和规则处理，不等于模型可以直接判定死亡。
- 叙事文本与结构化事实分开，便于回放、审计和修复。

## 6. 状态模型与存档设计

### 6.1 核心实体

建议至少包含以下表或等价的数据结构：

- `game_sessions`：存档名称、时代、状态、规则版本、创建时间、更新时间。
- `player_characters`：身份、外貌、背景、性格、价值观、血统和当前生命状态。
- `state_snapshots`：某一回合完成后的完整状态快照。
- `turns`：回合序号、玩家动作、模型输出、规则裁决、耗时和错误信息。
- `journal_entries`：面向玩家展示的纪事条目。
- `npcs`：原著 NPC 与动态 NPC 的统一档案。
- `relationships`：玩家-NPC关系，以及 NPC-NPC关系。
- `items` / `inventory_items`：物品定义和存档内持有数量、耐久、来源。
- `skills` / `character_skills`：技能定义和玩家熟练度。
- `timeline_events`：原著节点、状态、触发条件、改写记录和后果。
- `scheduled_events`：课程、信件、NPC 日程、待处理事件。
- `worldline_changes`：世界线变化率变化、原因和影响范围。
- `unlocks`：CG、收藏、成就、秘密和地点解锁。
- `llm_runs`：模型、提示词版本、输入输出摘要、token、耗时和错误，用于调试。

### 6.2 快照与事件日志并存

不能只保存“最新文档”：

- 事件日志记录每次动作和差异，适合纪事、审计和回放。
- 快照记录完整状态，适合快速加载。
- 每隔固定回合或关键事件创建快照。
- 允许玩家复制存档，复制时基于指定快照生成新会话。
- 运行时上下文使用“当前快照 + 近期事件 + 压缩摘要”，而不是完整聊天记录。

### 6.3 角色状态建议

玩家状态至少包含：

- 基础：姓名、年龄、生日、性别、性取向、血统、国籍、家庭关系。
- 生存：HP、MP、SP、精力、饱食度、伤势、状态效果。
- 四维：勇气、智慧、忠诚、野心。
- 技能：魔咒、变形、黑魔法防御、魔药、草药、天文、飞行、魔法史及选修技能。
- 资产：加隆、西可、纳特、物品、魔杖、宠物。
- 位置：时代、日期、时间、学年、年级、地点、当前活动。
- 关系：对每个已认识 NPC 的好感度、关系阶段、锁、最近互动和秘密。
- 世界线：变动率、已改写节点、未解决后果。
- 生命周期：正常、受伤、昏迷、死亡、毕业、退休、结局。

## 7. 规则引擎设计

### 7.1 时间系统

程序保存分钟级时间戳，并依据动作类型计算时间消耗。需要支持实时行动、快进日、快进周、暂停关键剧情、学年、季节、节日、满月、考试周和预定事件窗口。

LLM 可以提出时间消耗，但最终值由动作类型和规则表裁决。快进时不应只跳过时间，还要执行期间的课程、信件、NPC自治、健康变化和预定事件，并返回摘要。

### 7.2 技能与施法

建议把施法结果拆成：

```text
成功概率 = 基础熟练度
          + 属性修正
          + 魔杖适配
          + 环境修正
          + 精神/情绪状态
          - 咒语难度
          - 伤势与疲劳
```

具体公式和随机数由后端执行，随机种子写入回合，便于回放和测试。LLM 解释结果和描写过程，不生成不可复现的关键骰点。

### 7.3 冲突、战斗与死亡

战斗采用回合制或“动作—判定—后果”机制，不要求每次普通对话都进入战斗系统。

- 进入战斗前给出危险信号和可观察信息。
- 选项可以有风险标签，但不应暴露玩家角色无法知道的隐藏概率。
- 程序依据敌我状态、技能、环境、行动和随机种子计算结果。
- HP 降至 0 时进入濒死或昏迷；是否死亡需要结合伤害类型、救援机会、剧情规则和玩家此前行为判定。
- 死亡是可配置的结局，不由模型为了戏剧性随意触发。
- 存档在死亡后保留，可查看结局、复制死亡前快照或重新开始新人生。

### 7.4 关系、恋爱和 NPC 自治

好感度、关系阶段、表白门槛和血缘限制作为后端规则；LLM 负责生成符合阶段的行为与语言。

NPC 应有：

- 稳定的人格和背景。
- 按时间、地点和事件推进的日程。
- 目标、恐惧、秘密和当前情绪。
- 与玩家及其他 NPC 的关系边。
- 不在玩家视角中时产生的有限自治事件。

NPC 自治不需要每分钟模拟。可以在玩家快进、跨日、进入关键事件或主动查看相关 NPC 时结算，以控制成本。

对于原文中的“所有表白由 NPC 主动发起”，前端不提供“主动表白”按钮；玩家只能在表白事件发生后选择接受、拒绝或等待。关系规则要区分普通好感、信任、暧昧、恋爱、公开状态和多重关系惩罚。

## 8. Prompt 与上下文管理

### 8.1 提示词分层

每轮建议按以下优先级组织：

1. 系统安全与输出协议。
2. 世界不可变规则和 AI 禁令。
3. 当前时代包及临近原著节点。
4. 规则引擎给出的当前事实。
5. 玩家角色状态。
6. 当前场景、相关 NPC 和可见信息。
7. 最近若干回合。
8. 长期摘要、重要关系和未解决因果。
9. 本次玩家行动。

程序生成的事实应明确标记为“不可擅自修改”。玩家自由文本属于行动意图，不等于行动已经成功。

### 8.2 上下文压缩

当回合数增加时，使用三层记忆：

- 短期记忆：最近 5–10 回合的原文。
- 中期记忆：本章、本学期或本事件链摘要。
- 长期事实：角色、关系、物品、世界线和已确认秘密。

摘要必须由程序保存版本和来源，不能每次让模型自由改写全部历史。关键事实发生冲突时，以结构化状态和事件日志为准。

### 8.3 检索

MVP 阶段使用时代、地点、NPC、事件标签的数据库过滤即可。内容规模增长后再加入向量检索，用于寻找相关背景文本；向量检索只能提供参考材料，不能取代时间线和规则校验。

## 9. 前端信息架构

### 9.1 页面

- 存档列表：新建、继续、复制、重命名、删除、查看结局。
- 角色创建：13 轮向导、合法性校验、最终确认。
- 游戏主界面：剧情区、选项区、自由输入区、当前状态提示。
- 角色面板：基础背景、生存属性、四维、技能、物品、宠物、关系摘要、世界线和恋爱状态。
- 纪事面板：按时间线查看玩家选择、关键事件、后果、解锁和死亡记录。
- 关系详情：NPC 档案、关系阶段、互动记录、已知秘密和关系网络。
- 时间/通知/地图/课程等辅助面板。
- 设置：模型服务、输出风格、上下文长度、自动保存、导出存档。

原文写明“只有输入指令时显示面板”，而产品规划又要求常驻角色和纪事面板。建议采用兼容方案：角色和纪事作为可折叠、默认不打断剧情的侧栏；其余详细面板通过指令或按钮主动展开。这样既保留沉浸感，也满足网页游戏的信息可见性。

### 9.2 交互状态

- 生成中：显示叙事流式内容，但未提交前禁用重复行动。
- 等待选择：只允许当前回合的选项或“其他”自由文本。
- 风险事件：突出地点、状态和可见风险，不显示模型内部推理。
- 死亡/结局：停止继续输入，提供查看纪事、复制存档和新建人生。
- 断线恢复：通过回合 ID和存档版本重新拉取已提交结果。

## 10. API 草案

```text
POST   /api/sessions
GET    /api/sessions
GET    /api/sessions/{session_id}
POST   /api/sessions/{session_id}/duplicate
DELETE /api/sessions/{session_id}

GET    /api/sessions/{session_id}/state
GET    /api/sessions/{session_id}/journal
GET    /api/sessions/{session_id}/relationships
GET    /api/sessions/{session_id}/notifications

POST   /api/sessions/{session_id}/setup/answer
POST   /api/sessions/{session_id}/setup/confirm
POST   /api/sessions/{session_id}/actions
GET    /api/sessions/{session_id}/turns/{turn_id}
GET    /api/sessions/{session_id}/stream/{turn_id}

POST   /api/commands/parse
GET    /api/content/eras
GET    /api/content/options/{setup_step}
```

动作请求应包含：

```json
{
  "client_action_id": "uuid",
  "expected_state_version": 42,
  "kind": "choice",
  "choice_id": "choice_1",
  "free_text": null
}
```

## 11. LLM 服务适配与可靠性

Provider 配置至少支持：

- `base_url`
- `api_key`
- `model`
- `temperature`
- `max_tokens`
- `timeout_seconds`
- 是否支持 JSON Schema
- 是否支持流式输出

可靠性策略：

- 结构化输出失败时自动进行一次格式修复请求。
- 模型超时、限流和临时错误采用有限次数指数退避。
- 重试必须复用同一个回合上下文和动作 ID，不能重复推进状态。
- 达到重试上限时返回可解释的“主持人暂时无法继续”，不伪造状态变化。
- API key 只保存在服务端配置或加密存储中，不能下发浏览器。
- 日志默认脱敏，不记录完整 API key。
- 记录模型名、提示词版本、结构化响应和校验错误，便于定位问题。

## 12. 作弊指令与权限

作弊系统可以保留，但应定义为开发/个人模式：

- 默认关闭，创建存档时明确记录 `cheat_mode`。
- 每个作弊操作形成独立审计事件。
- 普通玩法和作弊玩法使用同一套状态结构，便于测试。
- “删除新 NPC”等不可逆操作需要二次确认，并优先改为软删除。
- 前端不把作弊指令和普通选项混在一起。
- 生产环境不允许通过模型文本触发作弊，必须经过明确的后端命令解析。

## 13. 内容和版权边界

该项目属于基于《哈利·波特》世界观的同人作品，商业化、公开分发、使用电影剧照/演员肖像、原著大段文本、电影素材和官方商标都可能涉及版权或商标问题。本规划不构成法律意见。

工程上建议：

- 使用原创 UI、美术、音乐和图标。
- 不把电影截图、官方海报或原著大段文本打包进项目。
- 将原著事实作为内部配置时控制引用范围，不复制整本作品。
- 对外发布前明确同人、非官方和非关联声明，并根据目标地区咨询专业法律意见。
- LLM 提示词要求“基于已配置的角色事实进行原创叙事”，不要要求复刻原文段落或电影台词。

## 14. 分阶段实施计划

### 阶段 0：规则收敛与垂直切片

目标是先做出一条完整可玩的最小链路，而不是一次实现所有系统。

- 确定一个时代，建议优先子世代。
- 确定一个学年开端和 3–5 个场景。
- 确定玩家创建流程的 13 轮协议。
- 把世界规则、NPC、地点、事件拆成最小结构化资料。
- 完成一轮“选择—LLM 叙事—结构化选项—状态保存—纪事展示”。

验收：创建新存档后可以完成角色创建、进行至少 10 个回合、刷新页面后继续，且上一轮的时间、物品和关系不会丢失。

### 阶段 1：MVP 核心可玩版本

- React 主界面和存档列表。
- FastAPI 会话、回合、快照和纪事 API。
- OpenAI-compatible provider。
- 角色面板和纪事面板。
- 时间、基础属性、技能、物品、关系。
- 标准化选项与“其他”输入。
- 失败重试、自动保存和断线恢复。

### 阶段 2：规则和剧情深度

- 课程、课表、考试、通知、信件。
- NPC 日程与有限自治。
- 声望、舆论、世界线变动。
- 关键原著节点和分支事件。
- 冲突、战斗、伤势、死亡和结局。

### 阶段 3：长期内容系统

- 四大时代包。
- 动态 NPC。
- 职业、毕业后人生和家庭。
- CG、收藏、成就、回忆模式。
- 地图、地点解锁、更多命令。

### 阶段 4：质量、运营和扩展

- 多用户鉴权、配额和费用统计。
- PostgreSQL、备份和导出。
- 提示词版本管理和 A/B 测试。
- 内容编辑工具。
- 观测、错误告警、模型切换和数据迁移。

## 15. 测试方案

### 15.1 规则单元测试

重点覆盖：

- 时间推进和快进期间事件结算。
- 属性、技能和施法判定。
- HP/MP/SP、伤势和死亡边界。
- 好感度区间、关系锁、表白门槛和拒绝后果。
- 血缘限制、作弊开关和权限。
- 世界线变化率和原著节点状态转移。
- 存档复制、快照恢复和回合幂等。

### 15.2 协议与集成测试

- 模型返回缺字段、错误类型、非法选项和多余文本时能否安全处理。
- 模型提出超出当前资源的物品、技能或时间变化时是否被拦截。
- LLM 超时、限流、断线、重复提交时状态是否只推进一次。
- 前端刷新、浏览器回退和 SSE 中断时能否恢复。

### 15.3 叙事回归测试

维护固定场景集，检查：

- NPC 性格和时代事实。
- 玩家选择没有被模型擅自替换。
- 每轮有合法选项和“其他”选项。
- 状态变化都有原因和事件来源。
- 不出现预知未来、超游、凭空施恩或抹除后果。

不应只用“文风好不好”作为质量指标，优先检查结构化事实一致性和规则合规性。

## 16. 主要风险与应对

### LLM 幻觉和状态漂移

应对：结构化状态、事件溯源、规则引擎二次裁决、短期上下文和固定事实注入。

### 上下文过长和成本失控

应对：分层记忆、摘要、标签检索、按场景注入 NPC，限制每轮最大上下文和输出长度。

### 模型输出不稳定

应对：JSON Schema、Pydantic 校验、修复重试、安全降级文本和模型能力配置。

### 动态 NPC 破坏世界一致性

应对：生成后经过后端档案校验，绑定时代、年龄、地点、血缘、日程和关系网络。

### 原著角色沦为工具人

应对：NPC 目标、日程、关系边和自治事件独立保存；模型每轮只获取相关角色，不让所有 NPC 都围绕玩家行动。

### 玩家自由输入带来不可控行为

应对：先识别行动意图和目标，再按当前状态裁决成功、失败或部分成功；不要把自由文本直接拼接为系统指令。

### 开发范围过大

应对：先完成单时代、单学年、少量 NPC 和有限事件的垂直切片，再逐步扩展四时代和全量系统。

## 17. 第一版建议的最小数据结构

初始实现可以先只做以下对象：

```text
Session
  id, name, era_id, status, state_version, created_at, updated_at

PlayerState
  identity, vitals, attributes, skills, inventory
  current_time, location_id, year_level
  relationships, worldline, pending_events

Turn
  id, sequence, player_action, narrative
  choices, proposed_changes, authoritative_changes
  state_version_before, state_version_after

Relationship
  npc_id, affinity, stage, trust, romance_state, known_secrets

JournalEntry
  timestamp, title, summary, player_choice, consequences, tags
```

先用这组对象跑通闭环，再引入课程、舆论、CG、职业和 NPC-NPC 关系网络。

## 18. 待确认事项

以下事项会影响数据模型和产品体验，建议在进入编码前确认：

- 首个可玩时代是否固定为子世代，还是四时代从第一版就都开放。
- 游戏是否只做单人本地运行，还是需要多用户账户和云端存档。
- 是否允许成人角色进入恋爱/婚姻内容；未成年阶段需要明确内容分级和边界。
- 玩家是否可以导入自定义世界观、NPC 和规则。
- LLM 是否允许联网检索；若允许，哪些内容可以作为事实来源。
- 是否需要图像生成、角色立绘和 CG，还是第一版纯文字界面。
- 作弊系统是否在普通用户版本中展示。
- 死亡后的存档策略：保留死亡结局、允许复制死亡前快照，还是支持回滚。
- 世界线变动率是单一数值，还是按时代节点、阵营和区域拆分。
- 原著正典与电影视觉设定冲突时，采用哪一套作为事实优先级。

## 19. 第一轮开发任务拆解

推荐的第一条开发链路：

1. 建立 FastAPI + React + TypeScript 项目骨架。
2. 定义 `Session`、`PlayerState`、`Turn` 和 `Choice` 的 Pydantic/TypeScript 类型。
3. 实现 13 轮角色创建向导和最终确认。
4. 将子世代开学前的少量设定导入内容表。
5. 实现一个 OpenAI-compatible provider。
6. 实现单回合 prompt builder 和 JSON Schema 输出。
7. 实现时间、物品、技能和关系的最小规则裁决。
8. 实现 SQLite 快照、回合日志和自动保存。
9. 实现主剧情区、选项区、自由输入和“其他”选项。
10. 用固定测试场景验证模型异常、重复提交和刷新恢复。

完成这条链路后，再决定是否扩展到战斗、课程和多时代。这样可以尽早验证最关键的问题：LLM 叙事质量、结构化输出稳定性、状态连续性和玩家是否真正感到自己的选择有效。

