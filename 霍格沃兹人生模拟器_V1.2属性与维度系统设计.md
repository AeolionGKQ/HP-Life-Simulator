# 霍格沃兹人生模拟器 V1.2 属性与维度系统设计

## 1. 文档信息

- 版本：V1.2 设计稿（剧情时间、地点与行动判定补充）
- 状态：V1.2 落地基准文档，按阶段实施并以本文档验收
- 适用范围：四个世代的统一 V1.2 系统
- 世代规则：邓布利多时代、亲世代、子世代和现代完全共用本属性、资源、关系、角色创建、生命周期和回合规则；世代只影响剧情背景、时代主线、可用人物与事件内容。
- 当前开放范围：仅子世代可选择；邓布利多时代、亲世代和现代保留在时代列表中，但在各自剧情内容完善前保持不可选择。
- 核心目标：建立一套名称明确、范围稳定、增减可审计、由程序最终裁决的角色属性与维度系统
- 设计原则：
  - 模型负责根据剧情提出变化建议，程序负责验证、裁剪、应用和记录。
  - 角色的“当前资源”与“长期能力维度”必须分离。
  - 每个可变数值都必须有固定 ID、显示名、取值范围和正向/负向变化场景。
  - 任何数值变化都通过结构化返回字段完成，不允许模型直接覆盖完整玩家状态。
  - 角色创建结束时只进行一次属性初始化请求；进入正常剧情后不再重复请求初始化模型。
  - 数值系统要能够解释“为什么变化”，也能够解释“为什么没有变化”。

---

## 2. V1.2 要解决的问题

当前 V1 的数值系统存在几个基础问题：

1. 当前资源和长期维度缺少统一目录、范围和清晰语义。
2. 模型可以提出数值变化，但缺少明确的合法属性目录、变化原因和变化速度。
3. 生命值归零还没有真正的死亡、结束和存档锁定流程。
4. 角色创建确认时没有单独生成一套初始属性，角色的初始强弱主要依赖固定默认值。
5. 属性变化的前端提示无法区分：
   - 一次性资源损耗；
   - 长期能力成长；
   - 属性上限提升；
   - 临时状态造成的有效能力变化。

V1.2 不只是增加五个字段，而是要建立一套可长期扩展的数值协议。

---

## 3. 系统总览

V1.2 将角色数值分为四层：

### 3.1 核心资源 Core Resources

代表角色此刻拥有多少可消耗或可恢复的生存资源：

- 生命值 `health`
- 魔力值 `mana`
- 精神值 `sanity`

核心资源可以在一个回合内频繁变化，直接受到受伤、施法、恐惧、休息、治疗和道具的影响。

### 3.2 辅助资源 Auxiliary Resources

保留当前系统已有的两个辅助计量：

- 精力 `energy`
- 饱腹度 `satiety`

它们不是用户提出的五项长期维度，也不参与角色初始天赋评级，但会影响剧情中的行动可行性和核心资源变化。

例如：

- 精力过低时，训练收益降低，体质相关行动更容易失败。
- 饱腹度过低时，精力恢复变慢，长时间可能导致生命值缓慢下降。

### 3.3 长期维度 Dimensions

代表角色的长期能力和倾向，默认范围为 0—20：

- 体质 `constitution`
- 智力 `intelligence`
- 精神 `willpower`
- 魅力 `charisma`
- 魔力 `magical_power`

维度不是当前体力条。一次受伤通常会减少生命值，而不是直接减少体质；只有长期伤势、疾病、诅咒、训练成果或重大成长才会改变维度。

### 3.4 临时与长期修正 Modifiers

以下内容不直接并入五项维度：

- 状态 `statuses`：如“发烧”“恐惧”“疲惫”“魔力枯竭”。
- 词条 `traits`：如“谨慎的阅读者”“龙焰灼痕”。
- 技能 `skills`：如“基础魔咒”“魔药学”。
- 装备和道具：如魔杖、护符、药剂。
- 声望、关系、环境和剧情条件。

它们通过修正检定结果、资源消耗、恢复速度或可选行动来影响属性表现，不与基础维度混为一谈。

---

## 4. 规范化数据模型

### 4.1 资源的统一结构

V1.2 的权威状态建议使用以下结构：

```json
{
  "resources": {
    "health": {
      "value": 100,
      "max": 100,
      "base_max": 100
    },
    "mana": {
      "value": 100,
      "max": 100,
      "base_max": 100
    },
    "sanity": {
      "value": 100,
      "max": 100,
      "base_max": 100
    },
    "energy": {
      "value": 100,
      "max": 100,
      "base_max": 100
    },
    "satiety": {
      "value": 100,
      "max": 100,
      "base_max": 100
    }
  }
}
```

字段含义：

- `value`：当前值。
- `max`：当前有效上限。
- `base_max`：未计算特殊背景和永久奇遇前的基础上限。

`max` 不允许因为普通资源增减自动变化。只有明确的背景、永久奇遇、身体改造或长期损伤才可以通过专门的上限变更字段修改它。

### 4.2 维度的统一结构

```json
{
  "dimensions": {
    "constitution": {
      "value": 10,
      "max": 20,
      "base_max": 20
    },
    "intelligence": {
      "value": 10,
      "max": 20,
      "base_max": 20
    },
    "willpower": {
      "value": 10,
      "max": 20,
      "base_max": 20
    },
    "charisma": {
      "value": 10,
      "max": 20,
      "base_max": 20
    },
    "magical_power": {
      "value": 10,
      "max": 20,
      "base_max": 20
    }
  }
}
```

规则：

- 维度当前值最小为 0。
- 普通上限为 20。
- V1.2 建议绝对上限为 30，供极少数传奇奇遇使用。
- 维度上限提升必须单独返回，不能通过普通 `dimension_deltas` 绕过。
- 维度当前值不能超过当前 `max`。
- 如果当前值已经达到 20，普通训练只能产生“经验已积累”或技能成长，不能继续增加维度。

### 4.3 属性目录

程序必须维护固定属性目录，模型只能使用目录中的 ID。

| 类型 | ID | 中文名 | 默认范围 |
| --- | --- | --- | --- |
| 核心资源 | `health` | 生命值 | 0—max |
| 核心资源 | `mana` | 魔力值 | 0—max |
| 核心资源 | `sanity` | 精神值 | 0—max |
| 辅助资源 | `energy` | 精力 | 0—100 |
| 辅助资源 | `satiety` | 饱腹度 | 0—100 |
| 长期维度 | `constitution` | 体质 | 0—20 |
| 长期维度 | `intelligence` | 智力 | 0—20 |
| 长期维度 | `willpower` | 精神强度 | 0—20 |
| 长期维度 | `charisma` | 魅力 | 0—20 |
| 长期维度 | `magical_power` | 魔力强度 | 0—20 |

“精神值”和“精神强度”必须在界面与提示词中使用完整名称，避免把 `sanity` 与 `willpower` 混称为“精神”。

---

## 5. 五项长期维度定义

### 5.1 体质 Constitution

体质代表身体强度、耐力、恢复能力和承受冲击的能力。

主要影响：

- 对物理伤害、跌落、寒冷、疾病和持续作战的抵抗。
- 需要体力的行动，如奔跑、攀爬、长时间飞行和近身对抗。
- 休息、治疗和训练后的恢复效率。
- 在相同伤害事件下，实际生命值损失。

增加场景：

- 有计划的体能训练、飞行训练和耐力训练。
- 长期康复并完成身体锻炼。
- 在安全条件下反复完成高强度体力任务。
- 获得强化体质的魔法、药剂、血统觉醒或永久奇遇。

减少场景：

- 严重伤势造成长期后遗症。
- 长期疾病、诅咒、营养不良或长期卧床。
- 被黑魔法永久侵蚀身体。
- 违反恢复要求，导致旧伤恶化。

不应减少的场景：

- 一次普通跌倒或短暂疲惫只减少生命值或精力，不直接减少体质。
- 一次训练失败不应直接减少体质。

### 5.2 智力 Intelligence

智力代表理解、推理、记忆、学习和分析复杂信息的能力。

主要影响：

- 解谜、调查、辨认线索和推理行动。
- 课程学习速度、知识吸收和复杂咒语理解。
- 分析魔药配方、古代魔文和魔法理论。
- 在信息不足时识别矛盾与谎言的能力。

增加场景：

- 系统学习课程、阅读高难度资料并完成练习。
- 独立解决复杂谜题或建立正确推理链。
- 在导师指导下掌握新的理论框架。
- 经历长期研究、记忆训练或特殊知识奇遇。

减少场景：

- 记忆损伤、精神侵蚀或影响认知的诅咒。
- 长期未经治疗的严重脑部伤害。
- 极少数改变认知能力的魔法事故。

不应减少的场景：

- 答错一道题、考试失利或暂时疲劳只影响本次检定，不直接减少智力。
- 被 NPC 欺骗不代表智力永久下降。

### 5.3 精神强度 Willpower

精神强度代表角色抵抗恐惧、压力、诱惑、精神攻击和长期挫折的能力。

主要影响：

- 抵抗摄魂、迷惑、恐惧、幻觉和精神控制。
- 在高压场景下保持行动能力。
- 精神值恢复速度和崩溃阈值。
- 需要长期专注、克服诱惑或坚持原则的行动。

增加场景：

- 冥想、心理恢复、稳定的休息和长期自我训练。
- 在危险中保持清醒并完成高压任务。
- 面对失败后进行反思、修复和重新建立信念。
- 获得守护神、精神防护或重大自我突破。

减少场景：

- 持续接触恐怖、精神控制、幻觉或黑魔法。
- 重大创伤、亲近者死亡或长期孤立。
- 反复透支精神值后仍不进行恢复。
- 被摄魂、梦魇或记忆篡改造成长期损伤。

不应减少的场景：

- 一次普通惊吓只减少精神值。
- 普通争吵、一次失败或短暂焦虑不应直接减少精神强度。

### 5.4 魅力 Charisma

魅力代表角色在表达、理解他人、建立信任、说服、哄骗和影响社交气氛方面的综合能力。

魅力不是单纯的外貌值，也不是 NPC 对角色的好感。它由先天条件、表达能力、仪态、穿着、自信、经验和社会环境共同表现。

主要影响：

- 说服、谈判、安慰、哄骗和公开表达。
- 初次见面时获得信任的概率。
- 在没有强制证据时影响他人态度的能力。
- 社交关系建立速度和部分好感变化幅度。

增加场景：

- 持续进行演讲、谈判、表演、社交和倾听训练。
- 成功安慰他人、化解冲突并总结交流经验。
- 获得正式礼仪、表演、心理沟通或领导训练。
- 长期维护良好声誉并形成稳定的自信。

减少场景：

- 长期社交退缩、严重创伤或表达能力受损。
- 公开丑闻造成长期自我否定并持续影响表现。
- 魔法事故导致声音、面容或感知能力长期受损。
- 被长期控制、羞辱或隔离，形成稳定的社交障碍。

不应减少的场景：

- 一次被拒绝、一次说谎失败或某个 NPC 不喜欢角色，不应直接减少魅力。
- 魅力降低必须有长期或明确的因果，不能成为模型惩罚玩家的通用字段。

### 5.5 魔力强度 Magical Power

魔力强度代表角色能够调动、承受和控制的魔法能量规模。

它不同于魔力值：

- 魔力值是当前可用的能量，类似“蓝条”。
- 魔力强度是角色长期能够达到的施法强度和上限能力。

主要影响：

- 可以稳定施放的魔法等级。
- 强力魔法的成功率、威力和范围。
- 高消耗魔法对魔力值的压力。
- 无杖魔法、守护神、变形和复杂咒语等高难度能力的成长上限。

增加场景：

- 年龄增长后进行安全的魔法训练。
- 完成课程学习、施法练习和导师指导。
- 成功控制高难度魔法并总结施法经验。
- 血统觉醒、魔法核心突破、特殊魔法遗物或重大奇遇。

减少场景：

- 魔力核心受损、魔法反噬或长期黑魔法侵蚀。
- 严重透支后强行施法，造成永久性损伤。
- 诅咒、魔法禁锢或失控事故导致长期能力下降。

不应减少的场景：

- 施法消耗只减少魔力值，不减少魔力强度。
- 普通咒语失败不应自动减少魔力强度。
- 魔力值归零只表示暂时无法继续施法，不表示能力永久消失。

---

## 6. 资源规则

### 6.1 生命值

生命值表示角色当前是否仍然存活。

增加场景：

- 治疗咒、医疗魔药、医疗翼治疗。
- 睡眠、充分休息和安全环境下的自然恢复。
- 低强度恢复性魔法或特殊治疗奇遇。

减少场景：

- 直接伤害、战斗、跌落、烧伤、溺水和疾病。
- 伤口恶化、毒药和诅咒。
- 饥饿、脱水或长期疲劳造成的间接损伤。

规则：

- `health` 始终裁剪到 `0 <= value <= max`。
- `health == 0` 时进入死亡结算。
- 生命上限提升不能通过普通治疗完成。
- 治疗不能在同一个普通回合中让已经死亡的角色复活。

### 6.2 魔力值

增加场景：

- 休息、睡眠和安全冥想。
- 饮用魔力药剂。
- 进入魔力充沛的环境或接受合法治疗。

减少场景：

- 施放任何需要消耗魔力的法术。
- 维持护盾、幻境、召唤和持续性魔法。
- 魔法反噬或被敌方抽取魔力。

规则：

- `mana == 0` 时不能执行需要魔力消耗的行动。
- 后端应根据行动声明的 `mana_cost` 拒绝明显不可执行的选择，或将其标记为不可用。
- 普通施法失败可以消耗魔力，但不能默认降低魔力强度。
- 魔力上限提升需要独立的 `resource_cap_deltas`。

### 6.3 精神值

增加场景：

- 睡眠、休息、冥想、放松和安全社交。
- 心理支持、守护神、治疗和完成创伤修复。
- 远离恐怖源、黑魔法源或持续压力环境。

减少场景：

- 看到恐怖场景、死亡、严重伤势和失控魔法。
- 遭遇精神攻击、幻觉、摄魂或长期噩梦。
- 连续劳累、睡眠不足、饥饿和孤立。

规则：

- `sanity == 0` 时进入崩溃或昏迷，不代表角色死亡。
- 后端设置 `lifecycle.status` 为 `collapsed` 或 `unconscious`，并写入恢复条件。
- 失能期间只能执行恢复、求助或由环境自动推进的安全事件。
- 精神值恢复到安全阈值后才能解除失能，默认安全阈值为最大值的 20%。

### 6.4 精力与饱腹度

这两个值继续保留，以兼容当前玩法，但在 V1.2 中明确为辅助资源。

精力增加：

- 睡眠、休息、食物和安全环境。

精力减少：

- 长时间行动、训练、奔跑、战斗、熬夜和高强度施法。

饱腹度增加：

- 正常进食、食物道具和恢复性魔法。

饱腹度减少：

- 时间流逝、体力活动和疾病。

辅助资源的耗尽不会直接结束游戏，但会产生状态或间接影响：

- 精力为 0：添加“极度疲惫”，行动难度增加，资源恢复减慢。
- 饱腹度为 0：添加“饥饿”，精力持续减少；长期不处理时生命值少量下降。

---

## 7. 维度范围与增减速度

### 7.1 取值等级

为了让模型和前端有共同语言，0—20 使用以下等级：

- 0—3：严重不足
- 4—7：偏弱
- 8—11：普通
- 12—15：良好
- 16—18：优秀
- 19—20：极高

21—30 仅用于明确的超常状态或传奇奇遇，不作为普通角色创建结果。

### 7.2 变化速度

长期维度不能像生命值一样每回合大幅波动。

默认限制：

- 单个普通回合，单项维度最多实际变化 `+1` 或 `-1`。
- 明确的长期训练、严重创伤或重大奇遇，单项维度最多实际变化 `+2` 或 `-2`。
- 传奇事件必须由程序标记为特殊事件后，才允许超过上述限制。
- 一次回合最多应用三项长期维度变化。
- 模型返回的超出部分不得四舍五入保留，必须记录为被裁剪的提议。

资源可以快速变化，但也要保留事件合理性：

- 普通伤害、施法和恢复按事件规模给出数值。
- 单次资源变化超过当前值 50% 时，必须有明确的重大事件原因。
- 后端可以允许致命伤害超过当前生命值，但最终值固定为 0。

### 7.3 属性上限变化

当前值变化与上限变化必须分开：

```json
{
  "resource_cap_deltas": [
    {
      "id": "health",
      "delta": 10,
      "reason_code": "permanent_blessing",
      "reason": "古老守护仪式永久增强了身体承受力"
    }
  ],
  "dimension_cap_deltas": [
    {
      "id": "magical_power",
      "delta": 2,
      "reason_code": "magical_awakening",
      "reason": "血统觉醒拓宽了角色能够承受的魔力上限"
    }
  ]
}
```

限制：

- 资源上限和维度上限不能由普通 `resource_deltas` 或 `dimension_deltas` 间接修改。
- 上限提升必须有明确的永久性原因。
- 上限降低代表永久损伤或诅咒，必须有重大剧情依据。
- 程序先应用上限变化，再裁剪当前值。
- 上限降低到当前值以下时，当前值同步裁剪到新上限，但不会额外产生一笔虚假的伤害。

---

## 8. 属性与剧情检定的关系

V1.2 需要明确属性如何影响事件，但不能让模型自行随意解释数值。

### 8.1 基本检定结构

后续可使用以下概念：

```text
有效能力 =
基础维度
+ 相关技能等级修正
+ 正面词条修正
- 负面状态修正
+ 装备、关系、环境和剧情修正
```

建议的属性对应：

- 体力、耐力、承伤：体质
- 推理、学习、调查：智力
- 恐惧、诱惑、精神攻击抵抗：精神强度
- 说服、安慰、哄骗、谈判：魅力
- 施法强度、复杂魔法控制：魔力强度

### 8.2 资源不替代维度

- 生命值低不等于体质低，但低生命值会产生受伤状态和行动惩罚。
- 魔力值低不等于魔力强度低，但魔力值不足时无法释放高消耗法术。
- 精神值低不等于精神强度低，但精神值低时更容易崩溃。

### 8.3 模型与程序的职责

建议的职责划分：

- 模型判断当前场景需要哪项维度、哪些资源变化以及变化原因。
- 程序根据维度、技能、状态和行动条件计算是否允许行动、实际资源消耗和最终数值。
- 模型负责把程序已经确定的结果写入自然语言剧情。
- 模型不得通过叙事文本声称“你获得了 3 点体质”而不返回结构化字段。

如果 V1.2 暂时不实现完整骰点，模型仍然必须返回 `checks` 建议，程序至少完成：

- 属性 ID 校验；
- 资源消耗条件校验；
- 变化范围校验；
- 结果和数值变化的一致性校验。

---

## 9. 模型返回协议

### 9.1 正常剧情回合

V1.2 使用以下语义明确的字段：

```json
{
  "resource_deltas": [
    {
      "id": "health",
      "delta": -12,
      "reason_code": "injury",
      "reason": "被失控的咒语击中肩膀"
    }
  ],
  "dimension_deltas": [
    {
      "id": "willpower",
      "delta": 1,
      "reason_code": "overcome_fear",
      "reason": "角色在恐惧中保持清醒并保护了同伴"
    }
  ],
  "resource_cap_deltas": [],
  "dimension_cap_deltas": []
}
```

每个正常剧情回合还必须在 `turn` 中返回当前剧情的绝对日期和地点：

```json
{
  "turn": {
    "current_date": "1991-09-03",
    "location_id": "hogwarts_library"
  }
}
```

- `current_date` 使用严格的 `YYYY-MM-DD` 格式，只精确到日期，不要求返回时分。
- 日期不能早于上一回合的 `current_context.current_date`；后端拒绝倒退日期。
- `location_id` 必须返回当前剧情地点。地点未改变时重复上一回合地点，改变时返回新的稳定地点 ID。
- 后端将这两个字段写入 `current_context`，下一回合原样作为上一轮时间和地点发送给模型。
- `time_advance_minutes` 不再作为新提示词要求的字段；旧数据仍可由后端兼容读取。

所有字段都使用相对变化量，不允许模型直接返回新的最终值。

### 9.2 字段规范

`resource_deltas`：

- 只允许 `health`、`mana`、`sanity`、`energy`、`satiety`。
- 正数表示增加，负数表示减少。
- `delta` 必须是有限数字。
- `reason_code` 必须来自程序允许的原因目录。
- `reason` 是给前端和审计记录看的简短解释。

`dimension_deltas`：

- 只允许五项长期维度。
- 正数表示成长，负数表示长期损伤或能力退化。
- 普通对话、普通移动和一次性失败不应返回长期维度变化。
- 必须同时填写 `reason_code` 和 `reason`。

`resource_cap_deltas` 与 `dimension_cap_deltas`：

- 只允许永久性变化。
- 不能在普通资源消耗中出现。
- 需要 `permanent: true` 或等价的程序可验证标记。
- 如果原因目录不允许永久变化，后端直接拒绝该项。

### 9.3 原因目录

建议第一版原因 ID：

资源原因：

- `injury`
- `healing`
- `spell_cost`
- `potion`
- `rest`
- `fear`
- `mental_attack`
- `fatigue`
- `hunger`
- `poison`
- `curse`
- `environment`

维度原因：

- `training`
- `study`
- `practice`
- `meditation`
- `social_experience`
- `overcome_fear`
- `major_discovery`
- `age_growth`
- `permanent_injury`
- `long_term_illness`
- `curse`
- `magical_awakening`
- `ritual`

原因目录用于校验和统计，不要求限制模型的叙事措辞。

### 9.4 选项效果预览

如果选项可能导致属性变化，必须在选择项中显式标注：

```json
{
  "effects": {
    "gains": [
      {
        "type": "dimension",
        "name": "精神强度",
        "description": "可能在克服恐惧后获得长期成长"
      }
    ],
    "losses": [
      {
        "type": "resource",
        "name": "精神值",
        "description": "可能因直面恐怖场景而下降"
      }
    ],
    "note": "结果取决于当前精神强度、状态和行动过程"
  }
}
```

预览可以使用“可能”，但回合实际应用必须只使用后端验证后的真实变化。

### 9.5 选项风险等级

模型生成的每个选项都必须包含 `risk` 字段，且只允许以下四个值：

| 返回值 | 前端显示 | 含义 |
| --- | --- | --- |
| `low` | 低 | 后果通常较轻或容易恢复 |
| `medium` | 中 | 可能产生明显损失、麻烦或关系影响 |
| `high` | 高 | 可能造成严重损失、处分、重大失败或危险 |
| `fatal` | 致命 | 可能导致角色死亡或不可逆的灾难性后果 |

风险不限于身体危险。以下情况也属于风险：

- 被朋友讨厌、关系恶化或失去信任；
- 被教授、家长或其他权威人物发现；
- 受到处分、失去资格或错过重要机会；
- 考试不及格、课程失败或声望受损；
- 损失物品、资源、线索或世界线机会。

模型只在 `risk` 字段中返回等级，不返回风险原因。前端以中文徽标显示等级；具体潜在后果仍可通过选项文案和效果预览表达。

### 9.6 行动结果与前置剧情条件

玩家在角色创建阶段选择的魔杖和学院，不等于故事中已经获得魔杖或完成分院：

- 在【奥利凡德魔杖店】剧情点之前，角色没有魔杖，不能使用魔杖施法或把创建时的选择当作已持有装备。
- 在【分院】剧情点之前，角色没有学院归属，不能使用学院公共区域、学院身份或学院声望。
- 只有相关剧情实际发生后，模型才能在叙事和结构化变化中确认魔杖获得或学院归属生效。

玩家选择的选项和自由输入的行动都不是必然成功。模型必须综合资源、五项长期维度、状态、词条、技能、装备、
年龄、关系、NPC 当前态度、环境、行动难度和既有剧情进行判断。结果可以成功、部分成功、失败或暂时没有实质结果。
失败必须在叙事中明确呈现，并可通过资源、状态、物品、关系、后续选项或世界线产生合理影响；不能为了迎合玩家而强行成功。

### 9.7 长期记忆重要度

当 `memory_update.create_long_term_memory` 为 `true` 时：

- `memory` 必须存在，并包含非空 `summary`。
- `memory.importance` 必须是 1 到 10 之间的整数。
- 禁止返回 `major`、`minor`、`high`、`low` 等文字等级。
- 模型返回不合规类型时，响应进入结构修复流程。
- 持久化层仍需对不可信输入安全归一化并裁剪到 1—10，任何异常值都不能打断回合请求。

---

## 10. 角色创建结束时的初始属性生成

### 10.0 最终确认前的角色补充步骤

原第 16 步“最终确认”顺延为第 18 步，并在学院选择之后增加：

1. 第 16 步“选择你的守护神”：
   - 提供海豚、猫、犬、马、雄鹿、牝鹿、水獭、野兔、狐狸和狼等预设，每项附带简短的沉浸式介绍。
   - 玩家可以不使用预设，直接输入自定义守护神形态。
   - 预设参考 Wizarding World 对守护神的基础设定：守护神通常呈动物形态，犬、猫、马属于较常见形态；具体形态因施法者而异，并可能随重大情感或人格变化而改变。
   - 创建时选择的只是角色潜在守护神形态。技能状态未明确记录已掌握【呼神护卫】时，角色不能召唤守护神，也不能把该选择当作已经拥有的施法能力。
2. 第 17 步“补充你自己”：
   - 提供自由输入框，允许玩家补充习惯、恐惧、愿望、秘密、口头禅、往事或任何其他角色设定。
   - 本步骤允许留空；留空不会阻止进入最终确认。
   - 原文作为角色信息保存，不由程序擅自改写或推导成数值优势。

守护神形态保存到 `player_state.patronus`，自由补充设定保存到 `player_state.character_notes`。两者都随完整玩家状态进入每一轮模型上下文，并在角色档案中可读展示。

### 10.1 触发时机

流程应调整为：

1. 用户完成姓名、性别、生日、学院、守护神和其他角色创建步骤，并可填写自由补充设定。
2. 用户点击“确认角色并开始”。
3. 后端确认角色设定并物化身份、出身、背景、性格、魔杖、天赋、宠物、守护神、自由补充设定和好友。
4. 后端单独请求一次 `attribute_initialization` 模型结果。
5. 后端校验并记录完整初始资源与维度。
6. 前端展示“角色初始属性”页面。
7. 用户点击“进入第一幕”。
8. 才允许发送第一轮正常 `narrative` 请求。

初始化请求不能和第一轮剧情请求合并。它不产生：

- 剧情正文；
- NPC 对话；
- 选择项；
- 世界线变化；
- 纪事；
- 长期记忆。

### 10.2 初始化状态机

建议在 `setup` 或独立的 `attribute_initialization` 节点中保存：

```json
{
  "attribute_initialization": {
    "status": "pending",
    "schema_version": "1.2",
    "request_id": null,
    "completed_at": null,
    "source": null,
    "error": null
  }
}
```

状态：

- `pending`：尚未请求。
- `generating`：正在请求模型。
- `ready`：已经成功生成并通过后端校验。
- `failed`：请求失败或返回不合规。

只有 `ready` 的存档可以开始第一轮剧情。

### 10.3 初始化请求输入

初始化模型只接收创建角色所需的权威设定：

```json
{
  "generation": {
    "id": "second_generation",
    "name": "子世代",
    "years": "1991—1998",
    "mainline": "..."
  },
  "character_setup": {
    "identity": {},
    "appearance": {},
    "family": {},
    "background": {},
    "personality": {},
    "values": {},
    "wand": {},
    "magic_talents": [],
    "pet": {},
    "starting_point": {},
    "selected_friends": []
  },
  "initial_state_constraints": {
    "age": 11,
    "resource_catalog": [],
    "dimension_catalog": [],
    "normal_dimension_max": 20
  }
}
```

不发送旧回合、纪事、世界线偏移或未来剧情，因为初始化必须只反映角色设定，不应提前污染故事。

### 10.4 初始化专用返回协议

返回必须是单独的 JSON 对象：

```json
{
  "response_type": "attribute_initialization",
  "schema_version": "1.2",
  "resources": [
    {
      "id": "health",
      "value": 100,
      "max": 105,
      "reason": "长期运动背景使角色拥有略高的基础生命上限"
    },
    {
      "id": "mana",
      "value": 92,
      "max": 110,
      "reason": "魔法天赋使角色拥有较高魔力储备"
    },
    {
      "id": "sanity",
      "value": 100,
      "max": 100,
      "reason": "角色当前精神状态稳定"
    },
    {
      "id": "energy",
      "value": 100,
      "max": 100,
      "reason": "故事开始时角色处于正常状态"
    },
    {
      "id": "satiety",
      "value": 100,
      "max": 100,
      "reason": "故事开始时角色没有饥饿问题"
    }
  ],
  "dimensions": [
    {
      "id": "constitution",
      "value": 11,
      "max": 20,
      "reason": "体格结实且有一定户外活动经历"
    },
    {
      "id": "intelligence",
      "value": 12,
      "max": 20,
      "reason": "好奇求知且有持续阅读经历"
    },
    {
      "id": "willpower",
      "value": 9,
      "max": 20,
      "reason": "目前缺少真正高压环境下的经验"
    },
    {
      "id": "charisma",
      "value": 10,
      "max": 20,
      "reason": "性格温和，社交能力处于普通水平"
    },
    {
      "id": "magical_power",
      "value": 13,
      "max": 20,
      "reason": "具有明确的先天魔法天赋"
    }
  ],
  "calibration_summary": "角色总体均衡，魔法潜力和求知能力较为突出。",
  "self_check": {
    "all_ids_valid": true,
    "all_values_in_range": true,
    "based_on_character_setup": true
  }
}
```

### 10.5 初始化数值校验

后端必须把模型结果视为提议，不直接信任。

资源校验：

- ID 必须属于固定资源目录。
- `0 <= value <= max`。
- `max` 必须在该资源允许的初始化范围内。
- 角色创建结束时不允许出现死亡或精神崩溃状态，除非角色设定明确包含严重伤病，并且 V1.2 明确允许这种起点。
- 常规起点默认所有资源当前值为满值；有明确背景依据时才允许低于上限。

维度校验：

- ID 必须属于五项维度。
- `0 <= value <= max <= 30`。
- 普通角色创建结果建议落在 4—16。
- 角色设定具有明确稀有背景时，才允许出现 17—20。
- 初始化结果必须覆盖全部五项维度，不能缺项。
- 初始化模型不能新增第六项基础维度。

一致性校验：

- 角色年龄、背景、体格、性格、天赋和魔杖必须能在返回理由中找到依据。
- 模型不能因为某项性格选择就直接生成传奇级全部属性。
- 总体不能出现五项维度同时达到 18 以上的结果。
- `reason` 只能用于解释，不作为程序状态事实。

### 10.6 初始化请求失败

同一次确认操作只允许发送一次初始化模型请求，不进行自动的第二次剧情请求或隐式补救请求。

失败时：

- 不进入第一轮剧情。
- 存档保留角色创建答案。
- `attribute_initialization.status` 设为 `failed`。
- 前端展示失败原因和“重新生成初始属性”按钮。
- 用户明确点击重试时，才重新发起初始化请求。
- 不得因为模型失败而把五项属性静默设为满值。

V1.2 不设置静默保底值。初始化失败时必须由用户明确重试，成功前不能开始第一幕。

---

## 11. 记录与审计

### 11.1 初始属性记录

初始化完成后必须同时保存：

1. 当前权威状态中的 `resources` 和 `dimensions`。
2. 初始化模型的原始结构化响应。
3. 后端裁剪前的提议。
4. 后端最终应用后的结果。
5. 初始化来源、时间、请求 ID 和协议版本。

建议增加独立的属性事件记录：

```json
{
  "event_id": "attribute-init-...",
  "session_id": "...",
  "turn_id": null,
  "phase": "character_creation",
  "source": "llm_initialization",
  "before": null,
  "proposed": {},
  "applied": {},
  "clamped": [],
  "created_at": "..."
}
```

### 11.2 普通回合记录

每次回合属性变化至少记录：

- 变化前值；
- 模型提议的 delta；
- 程序实际应用的 delta；
- 被裁剪或拒绝的字段；
- 原因 ID；
- 原因文本；
- 对应回合 ID；
- 是否触发状态、生命周期或结束。

这样可以区分：

- 模型提出 `-20`，程序实际应用 `-8`；
- 模型提出未知属性，被程序拒绝；
- 模型提出维度变化，但因本回合变化次数超限而被裁剪；
- 资源变化触发了死亡或精神失能。

---

## 12. 生命周期与结束规则

### 12.1 生命值归零

当最终应用后的 `health == 0`：

1. `lifecycle.status` 设为 `dead`。
2. 写入 `ending.type = "death"`。
3. 写入死亡原因、回合 ID、时间和地点。
4. 存档状态改为 `ended`。
5. 拒绝后续普通剧情行动。
6. 允许查看角色、纪事、关系和世界线，但不允许继续推进时间。

死亡由程序根据最终生命值裁决，不由模型返回的 `"dead": true` 决定。

### 12.2 精神值归零

当最终应用后的 `sanity == 0`：

1. `lifecycle.status` 设为 `collapsed` 或 `unconscious`。
2. 添加对应状态，例如“精神崩溃”或“昏迷”。
3. 当前回合可以正常封存叙事结果。
4. 后续只允许恢复、求助、治疗或安全环境相关行动。
5. 精神值恢复到最大值 20% 以上时，程序解除失能。

精神值归零不是游戏结束，但会显著改变可执行行动和叙事范围。

### 12.3 魔力值归零

魔力值归零不改变生命周期：

- 可以进行非魔法行动。
- 需要魔法的选项显示为不可用或标注“魔力不足”。
- 休息、药剂或剧情治疗可以恢复魔力。

---

## 13. 接口与流程设计

### 13.1 角色确认接口

现有的角色确认接口可扩展为两阶段响应：

```text
POST /api/sessions/{session_id}/setup/confirm
```

第一阶段完成：

- 验证全部创建答案；
- 物化角色状态；
- 写入 `attribute_initialization.status = generating`；
- 启动一次初始化请求。

请求完成后返回：

- `setup` 完成状态；
- 初始化状态；
- 完整初始资源；
- 完整五项维度；
- 是否允许进入第一幕。

如果不希望让确认接口保持较长连接，可以拆为：

```text
POST /api/sessions/{session_id}/setup/confirm
POST /api/sessions/{session_id}/attributes/initialize
GET  /api/sessions/{session_id}/attributes
```

但无论采用哪种形式，初始化请求都必须由后端控制幂等性，不能由前端在多个组件中重复触发。

### 13.2 第一幕接口限制

第一轮剧情请求，例如：

```text
POST /api/sessions/{session_id}/actions
```

在处理 `start_story` 前必须检查：

```text
attribute_initialization.status == "ready"
```

否则返回明确错误：

```text
角色属性尚未生成完成，暂时不能开始第一幕。
```

### 13.3 回合协议版本

建议在模型上下文中加入：

```json
{
  "protocol": {
    "name": "hp_simulator_turn",
    "version": "1.3"
  }
}
```

V1.2 只接受上述新协议。模型返回其他属性字段、旧字段或未登记字段时，整项返回视为不合规并进入修复/失败流程；不做旧协议映射。

---

## 14. 提示词设计要求

### 14.1 每轮必须发送

每次正常剧情请求都发送：

- 完整 `resources`；
- 完整 `dimensions`；
- 各资源上限；
- 当前状态、词条、技能、库存和装备；
- 当前生命周期状态；
- 最近回合、记忆和时代主线；
- 当前玩家行动。
- 上一回合返回的 `current_date` 和 `location_id`。

### 14.2 模型必须遵守

提示词中明确写出：

- 资源变化使用 `resource_deltas`。
- 长期成长使用 `dimension_deltas`。
- 施法消耗魔力值，不减少魔力强度。
- 普通受伤减少生命值，不直接减少体质。
- 恐惧事件减少精神值，长期创伤才可能减少精神强度。
- 资源为 0 的后果必须遵守系统规则。
- 不得新增未在目录中的属性 ID。
- 不得直接覆盖最终值。
- 每一项长期维度变化都必须有重大、明确、可追溯的场景原因。
- 不能为了制造戏剧性而随意降低维度。
- 玩家行动不保证成功，必须综合属性、状态、词条、技能、装备、关系、NPC 态度和环境判定。
- 失败可以产生惩罚、关系变化、资源损耗、状态影响或不同的后续剧情。
- 角色创建时的魔杖选择在【奥利凡德魔杖店】前不是已持有魔杖。
- 角色创建时的学院选择在【分院】前不是已生效的学院归属。
- 角色创建时的守护神选择只是潜在形态；未掌握【呼神护卫】时不能召唤，也不能视作已经获得该技能。
- 每轮必须读取并尊重 `character_notes` 中的玩家补充设定，但这些设定不能绕过技能、资源、年龄、前置剧情和程序权威状态。
- 每轮返回 `turn.current_date`（`YYYY-MM-DD`）和 `turn.location_id`，并以当前上下文的日期和地点为推进基准。
- 每个选项必须返回 `risk`，且只能为 `low`、`medium`、`high`、`fatal`；风险同时涵盖身体、社交、纪律、学业和机会损失。

### 14.3 变化与叙事一致性

模型返回的叙事、选项效果和结构化字段必须保持一致：

- 叙事写角色被咒语击中，字段应出现生命值下降。
- 叙事写角色饮用药剂，字段应出现魔力值或精神值恢复。
- 叙事写角色经过数月训练，才可以出现体质、智力或魔力强度增加。
- 叙事写角色只是“感到紧张”，优先减少精神值，而不是减少精神强度。

后端可以在审计中标记叙事与数值不一致，但不应依赖自然语言解析来裁决最终状态。

---

## 15. 前端展示设计

### 15.1 初始属性展示页

角色确认后，进入独立的“属性校准完成”页面：

- 显示角色姓名、时代和剧情起点。
- 显示五项维度的数值、等级和生成理由。
- 显示生命值、魔力值、精神值的当前值与上限。
- 显示精力和饱腹度作为辅助资源。
- 明确标注“由角色设定生成，后续会随剧情变化”。
- 提供“进入第一幕”按钮。

该页面不能展示尚未开始的剧情，也不应提前生成第一轮叙事。

### 15.2 角色面板

角色面板建议分成三个区块：

核心资源：

- 生命值：当前 / 上限
- 魔力值：当前 / 上限
- 精神值：当前 / 上限

辅助资源：

- 精力：当前 / 上限
- 饱腹度：当前 / 上限

长期维度：

- 体质
- 智力
- 精神强度
- 魅力
- 魔力强度

每个长期维度显示：

- 当前值；
- 当前等级；
- 与上次变化的差值；
- 最近一次变化原因；
- 是否达到当前上限。

### 15.3 剧情节点浏览

对话区底部提供“上一节点”和“下一节点”按钮，用于浏览已经生成并保存的剧情回合：

- 默认打开最新剧情节点。
- 点击“上一节点”查看更早的剧情，点击“下一节点”向最新剧情返回。
- 到达第一个剧情节点时，“上一节点”禁用并变灰。
- 到达最新剧情节点时，“下一节点”禁用并变灰。
- 历史剧情节点中的所有普通选项、自由输入框和提交按钮全部禁用并变灰，只允许浏览。
- 只有回到最新剧情节点后，玩家才能再次选择选项或输入自由行动。
- 浏览历史节点不修改玩家状态、状态版本、关系、纪事或世界线，也不触发模型请求。
- 日期、地点、世界线、剧情正文、选项和变化通知均显示当前正在浏览节点所记录的内容。

### 15.4 变化通知

剧情结束后显示程序实际应用的变化：

```text
本回合属性变化

生命值     -12    被失控咒语击中肩膀
魔力值      -8    施放护盾
精神值      +5    在安全环境中完成休息
精神强度    +1    在恐惧中保持清醒
```

如果模型提议被裁剪，普通玩家界面不需要展示技术细节，但可以显示：

```text
部分变化受到角色当前上限和规则限制。
```

详细审计页可以显示提议值、实际值和拒绝原因。

---

## 15.5 年级系统设计与落地要求

### 15.5.1 设计目标

年级是由程序掌握的权威学籍状态，不是模型可以自由覆写的叙事标签。模型每轮必须返回本回合结束后的年级；只有发生入学、升年级或离校事件时，才额外提出结构化学籍变更。程序结合当前状态、剧情日期和变更原因验证后决定是否应用。

四个世代共用同一套年级、课程与考试规则。世代只改变学年对应的公历年份、教授与学生名单、时代主线和具体事件。

### 15.5.2 权威年级枚举

| 年级 ID | 前端显示 | 说明 |
| --- | --- | --- |
| `not_enrolled` | 未入学 | 尚未完成霍格沃茨分院，或从未正式入学 |
| `year_1` | 1年级 | 已完成分院并进入第一学年 |
| `year_2` | 2年级 | 第二学年九月新学期开始 |
| `year_3` | 3年级 | 第三学年九月新学期开始 |
| `year_4` | 4年级 | 第四学年九月新学期开始 |
| `year_5` | 5年级 | O.W.L. 学年 |
| `year_6` | 6年级 | 已完成 O.W.L. 并继续在校 |
| `year_7` | 7年级 | N.E.W.T. 学年 |
| `left_school` | 已离校 | 已毕业、主动离校、辍学、被开除或因其他原因永久结束在校状态 |

`school.grade` 是唯一权威年级字段。旧字段 `school.year_level` 只允许在兼容读取时使用，不得继续由新代码写入，也不能覆盖 `school.grade`。

### 15.5.3 学籍状态结构

新存档和角色创建完成后的学籍结构：

```json
{
  "school": {
    "house": "gryffindor",
    "grade": "not_enrolled",
    "enrollment_started": false,
    "school_year": "1991-1992",
    "grade_started_year": null,
    "last_grade_promotion_key": null,
    "last_course_progression_year": null,
    "departure_reason": null,
    "owl_results": {},
    "newt_results": {},
    "elective_courses": [],
    "active_courses": []
  }
}
```

角色创建时选择的学院只是未来分院结果或角色倾向。即使剧情起点选择“分院时”，在分院剧情实际完成前仍保持：

```text
grade = not_enrolled
enrollment_started = false
```

`school.enrollment_started` 是只前进、不回退的入学里程碑：

- 初始为 `false`。
- 分院完成并被程序接受后变为 `true`。
- 此后即使角色离校，也保持 `true`。
- 该字段不能从 `true` 回到 `false`。

### 15.5.4 正常年级流转

允许的主路径：

```text
not_enrolled
  -> year_1
  -> year_2
  -> year_3
  -> year_4
  -> year_5
  -> year_6
  -> year_7
  -> left_school
```

具体规则：

1. `not_enrolled -> year_1`
   - 只能由“分院仪式实际完成”触发。
   - 程序接受后设置 `enrollment_started=true`。
   - 同时设置 `grade_started_year` 为完成分院时的公历年份。
   - 角色创建时选择学院、抵达礼堂或准备戴上分院帽都不足以触发入学。
2. `year_1 -> year_2` 至 `year_6 -> year_7`
   - 只能逐级前进，禁止跳级。
   - 年级只在九月新学期开始时改变，六月课程结算和七、八月暑假都保留原年级。
   - 程序触发条件为当前年级属于 `year_1—year_6`、`current_date.month == 9`，
     且 `current_date.year > school.grade_started_year`。
   - 每次只允许晋升一个年级。
   - 晋升后立即把 `grade_started_year` 更新为当前年份，并写入唯一的
     `last_grade_promotion_key = "{from_grade}:{to_grade}:{current_date.year}"`。
   - 同一九月的后续回合必须同时被 `grade_started_year` 和 `last_grade_promotion_key` 阻止重复晋升。
3. `year_5 -> year_6`
   - 除九月新学期条件外，必须已经完成 O.W.L. 考试。
   - 六、七年级课程必须根据 O.W.L. 成绩和教授要求筛选。
4. `year_7 -> left_school`
   - 正常毕业必须已经完成 N.E.W.T. 考试。
   - `departure_reason` 记录为 `graduated_after_newts`。
   - 这是毕业或永久离校，不属于九月自动晋升。
5. 禁止任何在校年级回退、跨级前进或从 `left_school` 返回学校。

### 15.5.5 非正常离校

以下情况允许从任意在校年级进入 `left_school`：

- `dropout`：主动辍学；
- `expelled`：被学校开除；
- `medical_departure`：因长期伤病或精神状态离校；
- `other_permanent_departure`：其他已经在剧情中明确成立的永久离校。

五年级完成 O.W.L. 后，允许使用 `left_after_owls` 表示不继续攻读 N.E.W.T. 课程。该原因只允许从 `year_5` 进入 `left_school`，并且必须已记录 O.W.L. 已完成。

临时回家、假期、停课、住院观察和短期休学不等于 `left_school`。这些情况应由地点、状态或剧情事件表达，不改变年级。

### 15.5.6 模型回合协议

回合协议版本升级为 `hp_simulator_turn/1.4`。`turn` 必须增加：

```json
{
  "turn": {
    "title": "本回合标题",
    "scene_type": "dialogue",
    "narrative": "本回合完整剧情正文",
    "current_date": "1991-09-01",
    "location_id": "hogwarts_great_hall",
    "grade": "year_1",
    "school_transition": {
      "type": "enrollment",
      "from_grade": "not_enrolled",
      "to_grade": "year_1",
      "reason": "sorting_completed",
      "evidence": "分院帽已经宣布学院，角色正式完成分院。"
    }
  }
}
```

没有学籍变化时：

```json
{
  "grade": "year_1",
  "school_transition": null
}
```

`school_transition.type` 只允许：

- `enrollment`
- `promotion`
- `departure`

`school_transition.reason` 只允许：

- `sorting_completed`
- `new_school_year_started`
- `graduated_after_newts`
- `left_after_owls`
- `dropout`
- `expelled`
- `medical_departure`
- `other_permanent_departure`

模型每轮都必须返回 `grade`。模型不能通过 `player_changes`、`state_proposals` 或叙事文字修改年级。
除一年级和离校外，九月晋升由程序根据日期和权威学籍状态触发；模型返回的晋升信息只用于叙事和协议对齐，
不能绕过九月条件，也不能阻止已经满足条件的程序晋升。程序完成裁决后，回包中的 `turn.grade` 必须规范化为权威年级。

### 15.5.7 程序裁决

程序处理每个回合时：

1. 读取并规范化当前 `school.grade`。
2. 校验模型返回的 `turn.grade` 是否属于权威枚举。
3. 对入学和离校 transition 校验 `from_grade`、`to_grade`、原因和前置条件。
4. 对 `year_1—year_6` 检查是否首次进入满足年份条件的九月，并计算唯一 `promotion_key`；
   九月晋升由程序触发，不要求模型必须正确提出 transition。
5. 非九月且没有入学或离校 transition 时，要求模型返回年级与当前权威年级一致。
6. 根据 O.W.L./N.E.W.T. 状态和允许的相邻路径决定是否应用。
7. 接受九月晋升后更新 `grade`、`grade_started_year`、`last_grade_promotion_key` 和 `school_year`，
   并把响应中的年级规范化为程序裁决结果。
8. 六月课程技能结算只更新课程技能与 `last_course_progression_year`，不得修改年级字段。
9. 拒绝后保留原年级，并在内部审计变化中写入请求值、保留值和拒绝原因。

至少使用以下拒绝原因：

- `grade_mismatch_without_transition`
- `transition_from_grade_mismatch`
- `transition_target_mismatch`
- `invalid_enrollment_transition`
- `grade_skip_not_allowed`
- `grade_regression_not_allowed`
- `promotion_requires_september_term`
- `promotion_already_applied_for_school_year`
- `promotion_requires_owl_completion`
- `graduation_requires_newt_completion`
- `left_after_owls_requires_owl_completion`
- `left_school_is_terminal`

### 15.5.8 入学前规则的生命周期

以下限制只在 `school.enrollment_started == false` 时注入回合系统提示词：

- 不得把角色创建时选择的魔杖视为已经获得；
- 在奥利凡德魔杖店剧情实际完成前不得使用魔杖；
- 不得把角色创建时选择的学院视为已经完成分院；
- 不得以学院成员身份进入公共休息室或使用学院声望；
- 根据纯血、混血、麻瓜出身限制角色对魔法界、霍格沃茨和来信的先验认知。

入学里程碑被程序接受后，上述整组“入学前叙事约束”从后续提示词中永久移除。不能只依赖当前 `grade` 判断，因为 `left_school` 角色也已经经历过入学。

以下守护神规则永久保留，不随入学而移除：

- `player_state.patronus` 只记录潜在守护神形态；
- 未在 `player_state.skills` 中明确掌握【呼神护卫】时不得召唤守护神；
- 选择守护神形态不等于自动拥有或学会【呼神护卫】。

### 15.5.9 原著课程约束

一、二年级以固定基础课程为主：

- 变形术；
- 咒语；
- 魔药；
- 魔法史；
- 黑魔法防御术；
- 天文学；
- 草药学；
- 一年级飞行课。

二年级结束前，学生至少选择两门三年级选修课：

- 算术占卜；
- 麻瓜研究；
- 占卜；
- 古代魔文研究；
- 神奇动物保护。

三、四年级学习基础必修课和已经选择的选修课。课程剧情不能让角色在没有选课、技能或合理前置经历时，突然以正式学生身份修读高阶专业内容。

五年级进行 O.W.L.：

- 通过等级：`O`、`E`、`A`；
- 不及格等级：`P`、`D`、`T`；
- 考试结果必须持久化到 `school.owl_results`；
- O.W.L. 成绩决定六、七年级可以继续修读的 N.E.W.T. 课程。

六、七年级进入高年级课程：

- 只能继续修读满足 O.W.L. 成绩门槛和教授要求的课程；
- 炼金术等专业课程只允许在最后两年、学校确实开课且需求足够时出现；
- 不能让低年级角色常规修读 N.E.W.T. 级课程。

七年级进行 N.E.W.T.：

- 考试结果持久化到 `school.newt_results`；
- 成绩影响毕业后的职业选择；
- 完成考试并离校后转为 `left_school`。

课程规则是剧情与行动前置约束，不新增第六项长期维度，也不能取代现有技能系统。课程学习成果通过技能、长期维度、考试记录和事件共同表达。

课程技能的年度自然成长与年级晋升分离：

- 每年首次进入六月时，视为当前学年课程完成；
- 六月只对当时仍在 `active_courses` 中的课程技能固定增加 `1` 级，不改变 `school.grade`；
- 课程技能等级上限为 `10`；
- 结算后写入 `last_course_progression_year=current_date.year`，同一六月后续回合不得重复增长；
- 七月和八月不结算课程技能，也不改变年级；
- 九月满足条件后才逐级改变年级；
- 已退课课程的技能条目和原等级保留，但因为课程不再位于 `active_courses`，之后六月不再自然固定成长；
- 重新选入同一课程时沿用已有技能等级，不重复创建或重置；
- 模型仍可在剧情中通过合法的普通技能增量提升已有技能，但不能把六月年度自然成长重复返回一次。

### 15.5.10 前端展示

角色面板必须显示权威中文年级：

```text
not_enrolled -> 未入学
year_1       -> 1年级
year_2       -> 2年级
year_3       -> 3年级
year_4       -> 4年级
year_5       -> 5年级
year_6       -> 6年级
year_7       -> 7年级
left_school  -> 已离校
```

课程面板继续展示完整 `school` 信息，并将 `grade`、`departure_reason`、O.W.L./N.E.W.T. 成绩、选修课和当前课程转换为可读中文。不得向玩家直接显示内部拒绝代码或协议字段说明。

### 15.5.11 测试与验收

后端至少覆盖：

- 新建存档和角色创建完成后为 `not_enrolled`；
- 完成分院后进入 `year_1` 且 `enrollment_started=true`；
- 无 transition 时模型返回不同年级会被拒绝；
- 分院前不能直接进入二年级；
- 一至六年级只能在满足年份条件的九月新学期逐级晋升；
- 六月课程结算后年级保持不变，七月和八月也不得晋升；
- `grade_started_year` 与 `last_grade_promotion_key` 能阻止同一九月重复晋升；
- 每个公历年六月只对当时的 `active_courses` 技能自然提升一次；
- `last_course_progression_year` 能阻止同一六月重复提升；
- 已退课课程技能保留，但不再参与六月自然成长；
- 禁止跳级、降级和从 `left_school` 返回；
- 五年级完成 O.W.L. 后可以进入六年级或选择离校；
- 未完成 O.W.L. 不得以 `left_after_owls` 离校；
- 七年级完成 N.E.W.T. 后正常离校；
- 未完成 N.E.W.T. 不得以毕业原因离校；
- 辍学、开除、长期伤病等永久离校可以被程序接受；
- 入学前提示词包含出身、魔杖和分院限制；
- 入学后提示词不再包含整组入学前限制；
- 守护神与【呼神护卫】限制在入学前、在校和离校后始终存在；
- JSON 模板中的 `grade` 和 `school_transition` 可被 Pydantic 正确解析。

前端至少覆盖：

- 角色面板分别显示“未入学”“1年级”和“已离校”；
- 课程面板可读地展示年级、考试和离校原因；
- 桌面端和移动端均无横向溢出；
- 历史剧情节点浏览不会改变当前权威年级。

---

## 16. 与状态、技能、词条和道具的边界

### 状态

状态是短期或中期效果，例如：

- 受伤
- 发烧
- 恐惧
- 魔力枯竭
- 精神崩溃
- 训练疲劳

状态可以修正属性检定和资源恢复，但不等价于维度变化。

### 技能

技能表示“学会了什么”，维度表示“能够以什么基础能力完成”。

每个技能同时保存熟练度等级和经验：

```json
{
  "level": 3,
  "experience": 45
}
```

- `level` 是技能熟练度，范围为 `0—10`；
- `experience` 是当前等级的成长进度，范围为 `0—100`；
- 模型可以通过 `skill_experience_deltas` 为已经存在的技能增加经验；
- 技能经验只允许增加，暂不支持模型减少经验；
- 经验达到 `100` 时，程序自动将 `level` 提升 `1` 并把 `experience` 清零；
- 自动升级和经验清零属于程序裁决，模型不得自行换算、清零或重复提交等价的等级提升；
- 单次经验增量即使超过升级阈值，也只提升一级，达到阈值后的溢出经验不保留；
- 技能已经达到 `10` 级时不再接受经验增长；
- 原有 `skill_deltas` 直接提升技能等级的机制继续保留，与经验成长机制并存；
- `skill_deltas` 和 `skill_experience_deltas` 必须分别对应剧情中真实发生的成长，不能对同一成果重复结算。

例如：

- 智力高但没有古代魔文技能：理解能力好，但仍缺少专业知识。
- 古代魔文技能高但智力普通：经过训练可以完成固定任务，但面对全新结构时优势较小。

### 词条

词条是稀有的长期标签，例如：

- “耐心的研究者”：提升学习和调查相关行动。
- “魔力反噬后遗症”：降低高强度施法的安全性。

词条不能被用来绕过维度范围。模型不能通过反复新增词条制造无限属性。

### 道具

道具可以恢复资源或提供临时修正：

- 魔力药剂：增加 `mana`。
- 安神药剂：增加 `sanity`。
- 营养食物：增加 `satiety` 和少量 `energy`。
- 强化护符：在特定场景提供临时体质或精神强度修正。

道具的临时效果不应直接永久增加维度，除非道具说明明确是永久奇遇或仪式。

---

## 17. 测试设计

### 18.1 属性目录测试

- 五项维度全部存在。
- 资源和维度 ID 不允许重复或未知。
- 资源与维度名称、范围、显示名一致。

### 18.2 初始化协议测试

- 初始化响应必须覆盖全部资源和全部五项维度。
- 缺失字段、未知 ID、字符串数字和超范围值必须被拒绝。
- 初始化请求成功后不能自动触发第一轮剧情。
- 同一个已经 `ready` 的存档重复确认不能重复初始化。
- 模型失败后存档停留在可重试状态，不可开始剧情。

### 18.3 资源增减测试

- 生命值受伤减少，治疗增加。
- 生命值不会低于 0 或超过上限。
- 魔法消耗魔力值，不减少魔力强度。
- 魔力值归零不能释放需要魔力的行动。
- 精神攻击减少精神值。
- 精神值归零进入崩溃或昏迷。
- 精神值恢复后可以解除失能。

### 18.4 五项维度测试

每项维度都至少需要覆盖：

- 合法增加；
- 合法减少；
- 超过当前上限；
- 低于 0；
- 未知 ID；
- 普通场景中不应变化；
- 重大场景中允许变化；
- 原因字段缺失；
- 单回合变化次数或幅度超限。

### 18.5 生命周期测试

- 生命值归零后存档变为结束状态。
- 死亡存档不能继续请求普通剧情。
- 死亡存档仍可查询角色和纪事。
- 精神值归零不结束游戏。
- 魔力值归零不结束游戏。

### 18.6 提示词和协议纯度测试

- 每轮提示词包含完整资源和五项维度。
- 提示词要求模型使用 V1.2 字段。
- 提示词和所有 JSON 样例中不出现旧属性字段或旧变更字段。
- 模型返回未知属性时后端拒绝或记录为裁剪。

---

## 18. 分阶段落地建议

虽然本阶段不实施，但建议后续按以下顺序落地：

### 阶段一：数据与协议

- 增加规范化资源和维度目录。
- 增加 `resources`、`dimensions` 和初始化状态。
- 扩展 Pydantic 协议。

### 阶段二：初始化流程

- 增加初始化提示词和专用模型响应解析。
- 在角色确认后执行一次初始化请求。
- 增加失败、重试、幂等和初始化审计。
- 阻止未初始化存档开始第一幕。

### 阶段三：回合规则

- 用 `resource_deltas` 和 `dimension_deltas` 替换旧字段。
- 实现范围裁剪、变化速度、原因目录和上限变化。
- 实现死亡、精神失能、魔力不足和辅助资源规则。

### 阶段四：前端展示

- 增加初始属性展示页。
- 重做角色面板的资源和维度区块。
- 增加回合变化通知和属性历史。
- 增加死亡结局和精神失能界面。

### 阶段五：模型质量和内容

- 为每项属性补充正向、负向和不应变化的剧情样例。
- 为课程、战斗、探索、关系和休息场景补充属性变化模板。
- 增加模型输出对抗测试，防止属性膨胀和无理由削弱。

---

## 19. 最终验收标准

V1.2 属性系统完成后，应满足：

1. 玩家能在角色创建结束后看到一套完整初始属性。
2. 初始属性来源于一次独立的模型请求，并被记录。
3. 第一轮剧情不会与属性初始化请求混在一起。
4. 五项长期维度全部有明确的增加场景和减少场景。
5. 核心资源、辅助资源和长期维度在数据结构和 UI 上清晰分离。
6. 模型只能返回相对变化量，不能直接覆盖程序状态。
7. 未知属性、非法范围和过快成长会被后端拦截或裁剪。
8. 每次实际变化都能追溯到回合、场景、原因和变化前后数值。
9. 生命值归零会结束游戏，精神值归零会进入可恢复失能，魔力值归零只限制施法。
10. 四个世代使用完全相同的属性、资源、生命周期和回合协议，只有剧情上下文不同。
11. 前端能明确展示当前值、上限、长期维度和最近变化。
12. 属性系统不会取代技能、状态、词条、关系和装备，而是与它们形成清晰分工。

---

## 20. 当前落地边界

- V1.2 只创建新版本状态结构，不提供旧存档迁移或旧协议兼容。
- 新存档从 `resources`、`dimensions` 和 `attribute_initialization` 开始。
- 模型提示词、返回字段和样例只使用 V1.2 新协议。
- 四个世代共用全部系统，世代只注入剧情相关上下文。
- 落地顺序以数据协议、初始化流程、回合规则、前端展示、测试自查为准。
