import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hpsimulator.app',
  appName: 'Hogwarts Life Simulator',
  webDir: 'dist'
};

export default config;
