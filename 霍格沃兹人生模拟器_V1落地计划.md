# 霍格沃兹人生模拟器 V1 落地计划

版本：V1.0 规划稿  
依据：`霍格沃兹人生模拟器.docx`、`霍格沃兹人生模拟器_系统规划.md`  
目标：实现一个可完整游玩的、纯文本、单人、本地存档的子世代人生模拟器

## 1. V1 已确认的产品边界

### 1.1 游戏范围

V1 只实现子世代时间线，即哈利·波特在霍格沃茨就读的主要时期：

- 起始时间：1991 年 9 月前后。
- 主要时间范围：1991–1998 年。
- 主要场景：霍格沃茨、霍格莫德、对角巷、禁林、国王十字车站、魔法部及与子世代相关的其他地点。
- 主要 NPC：哈利、罗恩、赫敏、德拉科、纳威、金妮、卢娜、塞德里克、教师、凤凰社成员、食死徒及其他子世代相关角色。
- 主要原著节点：魔法石、密室、阿兹卡班的囚徒、火焰杯、凤凰社、混血王子、死亡圣器及霍格沃茨之战。

V1 不实现其他三个时代的可玩内容，但数据结构要保留 `era_id`，未来可以在不重写存档和规则系统的情况下增加时代包。

### 1.2 运行和存储范围

游戏是本地单人应用：

- 前端、后端和数据库运行在玩家本机。
- 存档、日志、设定、提示词和模型响应默认保存在本机。
- 不实现账号、云存档、多人房间、联机同步、在线排行榜或服务端用户系统。
- 仅在调用外部 LLM API 时产生网络请求。
- 允许配置本机运行的 OpenAI-compatible 模型服务，因此可以做到完全离线游玩。
- 不生成图像、不依赖图片素材，所有内容以文本和结构化 UI 呈现。

这里的“完全本地”指游戏数据和应用服务不上传到项目服务器；如果使用外部模型 API，发送给模型的内容会按照所配置的服务商隐私策略处理。V1 应在设置界面明确提示这一点。

### 1.3 V1 的完整系统范围

V1 初步包含设定文档中的全部系统，但各系统先实现“可用基础版”：

- 13 轮角色创建。
- 子世代时间线和原著关键节点。
- 时间、学年、季节、节日、满月、考试周。
- 霍格沃茨地点、地图和可探索区域。
- 课程、课堂互动、成绩和选修课。
- 玩家属性、技能、魔杖、物品、金钱和宠物。
- NPC 档案、NPC 日程、NPC-NPC 关系和有限自治。
- 玩家与 NPC 的好感度、关系阶段、秘密和关系锁。
- NPC 主动表白、恋爱、拒绝、等待和分手。
- 未成年恋爱年龄限制。
- 学院声望、魔法界声望、阵营声望、舆论和传闻。
- 世界线偏移率和原著节点改写。
- 遭遇、冲突、决斗、伤势、濒死、死亡和结局。
- 新 NPC 动态生成。
- 血缘限制和作弊模式。
- 信件、通知、收藏、回忆、CG 文本、成就。
- 角色面板、纪事面板、关系详情、时间和地图等查询面板。
- 前端查询菜单：角色、纪事、恋爱、好感、声望、课程、收藏、信件、地图等。
- AI 禁令、自检结果和模型输出校验。

“包含”不代表 V1 一开始就有所有内容的完整数量。每个系统先以最小可运行实现接入主循环，再通过游玩测试扩充内容。

## 2. V1 核心设计原则

### 2.1 LLM 是主持人，程序是存档裁判

程序负责保存和验证确定性事实：

- 当前时间、地点、学年、年级和活动。
- 角色完整属性、技能、状态、背包、金钱和宠物。
- 所有已生成 NPC 的完整档案和关系。
- 玩家-NPC、NPC-NPC 关系数据。
- 课程、成绩、通知、信件和待处理事件。
- 原著节点、世界线偏移记录和已触发后果。
- 战斗结果、伤势、死亡和结局状态。

LLM 负责：

- 叙事、环境描写和对话。
- NPC 的语言表达和当下反应。
- 将玩家自由文本转为待裁决的行动意图。
- 根据当前状态设计遭遇和选项。
- 每轮给出世界线偏移率及其叙事原因。

模型不能通过自然语言直接修改数据库，也不能把玩家没有选择的行为写成已经发生的事实。

### 2.2 每轮传入完整的当前状态

用户明确要求每轮确保传入完整角色信息、当前状态和人物关系。V1 采用以下方式实现：

- 每轮都从数据库读取完整 `PlayerState`，不只依赖上一轮摘要。
- 每轮都传入完整的当前时间、地点、学年、年级、生命状态、属性、技能、物品、金钱、宠物、已知秘密、声望、世界线、待处理事件和解锁记录。
- 每轮都传入当前存档中已建立的完整关系表，包括玩家-NPC关系和已建立的 NPC-NPC 关系。
- 每轮都传入所有已生成 NPC 的完整档案，至少包括基础信息、人格、背景、目标、恐惧、日程和关系摘要。
- 近期叙事原文只保留有限窗口；完整历史通过事件日志、关键事实和阶段摘要传入。

这里的“完整”指完整的当前结构化状态，不是把全部历史小说原文无限追加到上下文。这样既满足状态追踪要求，也避免上下文长度随着游玩时间无限增长。

### 2.3 关键状态必须可追溯

每次状态变化都要能回答：

- 是哪一轮玩家行动导致的？
- 是模型提出的，还是规则系统补充的？
- 变化前后是多少？
- 变化的原因是什么？
- 是否触发了事件、解锁、舆论或世界线后果？

因此 V1 同时保存：

- 回合日志。
- 状态差异。
- 完整状态快照。
- 面向玩家的纪事。
- LLM 原始结构化响应和校验结果。

### 2.4 长期事件记忆

V1 采用“四层记忆 + 两级召回”方案，解决短期剧情连续性和长期事件追踪问题。

四层记忆：

1. **权威当前状态**：每轮从数据库读取并完整传给 LLM，包括玩家、NPC、关系、物品、时间、世界线和待处理事件。
2. **短期原文**：默认保留最近 10 个叙事回合的完整原文，并设置 token 上限；达到上限时从最早回合移除。
3. **长期事件记忆**：为重要事件生成结构化记忆条目，并保留其对应的回合原文引用。
4. **阶段摘要**：保存当前章节、学期和学年的宏观剧情摘要、关键因果和未解决伏笔。

所有回合原文都必须写入 `TurnRecord`，但不要求每个普通日常回合都创建长期记忆。以下内容应优先创建长期记忆：

- 秘密、承诺、谎言、背叛和重要信息。
- 重要物品的获得、丢失、使用和来源。
- 关系阶段变化、表白、拒绝、分手和重大冲突。
- 主线、支线、世界线节点的开启、改写和结算。
- 伤势、濒死、死亡、救援和重大危险。
- CG、成就、重要地点和关键人物的解锁。
- 玩家明确要求保留的事实。

长期记忆不能只有一句模糊总结，至少应包含：

```json
{
  "memory_id": "MEM-1991-0042",
  "title": "与赫敏共同隐藏违禁书",
  "summary": "玩家帮助赫敏将违禁书藏在图书馆东侧书架后，并承诺不向教授透露。",
  "time": "1991-10-18T00:20:00",
  "location_id": "hogwarts_library",
  "actors": ["player", "hermione_granger"],
  "event_type": "shared_secret",
  "importance": 7,
  "status": "open",
  "keywords": ["违禁书", "承诺", "图书馆", "秘密"],
  "facts": [
    "赫敏知道玩家愿意为她违反校规",
    "玩家答应不透露违禁书的存在"
  ],
  "open_threads": ["违禁书仍未取回"],
  "resolved_threads": [],
  "source_turn_ids": ["TURN-0041", "TURN-0042"]
}
```

长期记忆的原文不另存为松散文档，而是通过 `source_turn_ids` 关联 `TurnRecord` 中的完整剧情。这样可以同时保留简短可检索的摘要和可按需查阅的原始内容。

两级召回：

- **后端自动召回**：玩家提交行动后，后端根据人物、地点、物品、关键词、当前任务、开放伏笔和世界线节点，自动召回约 3–8 条相关长期记忆，放入首次 Prompt。
- **LLM 主动补查**：如果模型判断摘要不足以安全推进剧情，可以返回 `memory_request`，指定需要查阅的 `memory_id`。后端读取对应回合原文，加入同一回合上下文后重新调用模型。

主动补查是内部过程，不显示在前端，不推进时间，不修改状态，不生成纪事。每个玩家回合最多允许一次补查，最多查阅 3–5 条记忆；第二次模型调用必须返回正式剧情回复，以避免无限循环。

长期记忆目录不无限全部传入模型：

- 始终传入：当前章节的重要事件、所有未解决事件、当前人物相关的关键记忆、世界线改写节点和长期承诺。
- 自动召回：当前地点历史、当前物品来源、较早的人物互动和已解决但可能被重新提起的事件。
- 仅存数据库：与当前剧情无关的普通日常及完整原文。

V1 第一阶段采用 SQLite 的结构化标签和关键词检索，不引入向量数据库。后续当内容规模和表达方式需要时，再增加向量检索作为辅助，且不能替代结构化状态和事件规则。

## 3. 本地应用架构

### 3.1 推荐形态

V1 采用“本地 FastAPI 服务 + 浏览器 React 前端”的形态：

```text
玩家浏览器
    │ localhost HTTP/SSE
    ▼
FastAPI 本地服务
    ├── 角色创建与会话管理
    ├── 回合编排
    ├── 规则引擎
    ├── Prompt 构造
    ├── LLM Provider 适配
    └── SQLite 存档
            │
            └── 可选：外部或本机 OpenAI-compatible API
```

V1 不需要微服务、消息队列、Redis、云数据库或 WebSocket。

### 3.2 技术栈

- 前端：React、TypeScript、Vite。
- 前端数据请求：TanStack Query。
- 前端本地 UI 状态：Zustand。
- 后端：Python、FastAPI、Pydantic v2。
- ORM：SQLAlchemy 2.x。
- 数据库：SQLite。
- 数据迁移：Alembic。
- LLM 客户端：兼容 OpenAI Chat Completions/Responses 风格的适配层。
- 流式传输：SSE。
- 测试：pytest、httpx、Vitest、React Testing Library。

V1 先使用单进程运行。前端生产构建后由 FastAPI 提供静态文件，玩家通过一个启动命令打开本地游戏。

### 3.3 建议目录结构

```text
hp-simulator/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── services/
│   │   ├── rules/
│   │   ├── prompts/
│   │   ├── providers/
│   │   ├── content/
│   │   └── main.py
│   ├── migrations/
│   └── tests/
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   ├── components/
│   │   ├── features/
│   │   ├── stores/
│   │   ├── api/
│   │   └── types/
│   └── tests/
├── data/
│   ├── game.db
│   ├── content/
│   └── exports/
├── config/
│   └── settings.example.toml
└── scripts/
```

`data/` 下的游戏数据属于用户本地存档，不应默认加入版本控制。

## 4. V1 数据模型

### 4.1 存档

`GameSession`

- `id`
- `name`
- `era_id = "second_generation"`
- `rule_version`
- `content_version`
- `status`：`setup`、`active`、`dead`、`ended`
- `state_version`
- `created_at`
- `updated_at`

每个存档完全独立。存档之间不共享玩家状态、关系、事件和世界线。

### 4.2 玩家完整状态

`PlayerState`

- 身份：姓名、性别、生日、年龄、性取向、血统、国籍。
- 外貌：身高、体型、发色、瞳色、显眼特征。
- 家族：父母、兄弟姐妹、家族财富、家庭关系、血缘图谱。
- 童年经历和性格倾向。
- 信仰、价值观、人生目标、最深恐惧。
- 学院、年级、学年、当前课程状态。
- HP、MP、SP、精力、饱食度、伤势和临时状态。
- 勇气、智慧、忠诚、野心。
- 全部技能熟练度。
- 魔杖属性和适配信息。
- 加隆、西可、纳特。
- 背包、装备、收藏品和宠物。
- 当前日期、时间、时段、地点和活动。
- 学院、学术、社交、战斗、道德、领导和黑魔法声望。
- 当前恋爱状态、表白等待、关系限制。
- 世界线偏移率、偏移原因和已改写节点。
- 已知秘密、已解锁 CG、成就、通知和信件。
- 生命周期状态：正常、受伤、濒死、死亡、毕业、结局。

### 4.3 NPC

`NPCState`

- 稳定 ID、是否原著 NPC、姓名和时代。
- 年龄、生日、性别、性取向、血统、学院、年级。
- 外貌、气质和叙事描述。
- 核心性格维度。
- 家庭背景、目标、恐惧、秘密。
- 当前地点、日程、情绪和健康状态。
- 好感度、关系阶段、信任、恋爱状态和关系锁。
- 送礼偏好。
- 与玩家的关系日志。
- 与其他 NPC 的已建立关系。
- 可攻略限制、血缘关系和内容分级状态。
- 新 NPC 来源、生成原因和生成时的世界线。

### 4.4 长期记忆和阶段摘要

`MemoryEvent`

- `memory_id`
- `session_id`
- 标题、简短摘要和事件类型。
- 发生时间、地点、参与者和关键词。
- 重要度、状态（开放/已解决/被替代）。
- 结构化事实。
- 未解决伏笔和已解决伏笔。
- `source_turn_ids`：对应完整原文所在的回合。
- 关联 NPC、物品、任务和世界线节点。

`StorySummary`

- `summary_id`
- `session_id`
- 摘要范围：章节、学期或学年。
- 当前阶段摘要。
- 关键因果链。
- 当前开放伏笔。
- 生成时覆盖的回合范围。
- 摘要版本和更新时间。

### 4.5 回合和状态差异

`TurnRecord`

- `turn_id`
- `session_id`
- `sequence`
- `state_version_before`
- 玩家动作类型：选项、自由文本、快进。
- 玩家动作内容。
- Prompt 版本和模型信息。
- LLM 原始结构化响应。
- 主持人叙事文本。
- 返回选项。
- 模型建议的状态变化。
- 规则引擎最终裁决的状态变化。
- 世界线偏移率及原因。
- AI 自检结果。
- `state_version_after`
- 错误和重试信息。

### 4.6 事件日志和快照

V1 每轮都写入：

- 一条不可变回合记录。
- 一条或多条状态差异。
- 一份当前完整状态快照。
- 一条面向玩家的纪事。

快照用于快速加载，事件日志用于回放、调试、导出和复制存档。

## 5. LLM 回合协议

### 5.1 固定返回结构

LLM 每次只能返回两种互斥类型之一：正式剧情或内部记忆查阅请求。

正式剧情要求返回以下固定顶层字段：

```json
{
  "response_type": "narrative",
  "turn": {
    "title": "天文塔下的风",
    "scene_type": "dialogue",
    "narrative": "……",
    "location_id": "astronomy_tower",
    "time_advance_minutes": 15
  },
  "choices": [],
  "state_proposals": {},
  "worldline": {
    "offset_rate": 0.0,
    "delta": 0.0,
    "reason": "本轮没有改变已知原著节点",
    "affected_nodes": []
  },
  "events": [],
  "memory_update": {
    "summary": "本轮简短剧情总结",
    "create_long_term_memory": false,
    "memory": null,
    "resolved_memory_ids": []
  },
  "self_check": {}
}
```

如果模型认为摘要不足以继续推进，则返回：

```json
{
  "response_type": "memory_request",
  "memory_request": {
    "memory_ids": ["MEM-1991-0042"],
    "reason": "需要确认玩家此前对赫敏作出的承诺和违禁书的藏匿位置"
  }
}
```

`memory_request` 是后端内部响应，不发送到前端。收到后，后端读取所指记忆关联的完整回合原文，加入本次 Prompt，并进行第二次模型调用。该过程不能推进时间、修改状态或写入玩家纪事。

每个玩家回合最多允许一次补查，一次最多查阅 3–5 条记忆。补查后的第二次模型调用必须返回 `response_type = "narrative"`。

### 5.2 世界线偏移率

按照用户确认，`worldline.offset_rate` 是每轮都返回的字段，并由 LLM 根据本轮剧情判断：

- `offset_rate`：当前世界线偏移率，建议范围 `0–100`。
- `delta`：相对于上一轮的变化量。
- `reason`：变化原因。
- `affected_nodes`：可能受到影响的原著节点 ID。

前端每轮在角色面板和剧情摘要区域显示当前值。

后端仍需做最小限度的安全处理：

- 校验字段存在、类型正确、数值在 `0–100`。
- 越界值截断或要求模型修复。
- 将模型返回值和上一轮值同时保存。
- 不擅自替模型重新计算该数值。
- 对同一事件的异常大幅跳变记录警告，供测试和后续规则调整。

V1 将该字段视为“LLM 主导的叙事指标”，不是完全确定性的游戏属性。后续实际测试后再决定是否增加程序规则、分节点偏移率或单调性约束。

### 5.3 选项要求

- 每轮至少返回 2 个普通选项。
- 最后一个选项必须是 `kind = "free_text"` 的“其他”。
- 每个选项拥有稳定 ID。
- 选项描述只能包含玩家当前可知的信息。
- 不得把隐藏概率、未来必然事件或模型内部推理泄露给玩家。
- 选择权只属于玩家；模型不能在叙事中替玩家执行尚未选择的行动。

### 5.4 状态建议与程序状态

LLM 返回 `state_proposals`，包含：

- 时间推进建议。
- 关系变化建议。
- 技能和属性变化建议。
- 物品变化建议。
- 事件和秘密建议。
- 伤势和战斗结果建议。

V1 的处理策略：

1. 模型提出建议。
2. 后端检查对象是否存在、数值是否合理、是否满足当前条件。
3. 规则模块生成最终差异。
4. 差异写入回合记录和状态快照。
5. 如有冲突，规则结果覆盖模型建议，并让模型根据最终结果生成或修订叙事。

### 5.5 记忆更新

正式剧情每轮都返回 `memory_update.summary`，作为本轮极简剧情总结。后端根据模型建议和规则判断决定是否创建或更新 `MemoryEvent`：

- 普通日常回合只保存极简总结和完整原文，不创建永久长期记忆。
- 重要事件设置 `create_long_term_memory = true`，并返回完整结构化 `memory`。
- 事件解决时通过 `resolved_memory_ids` 标记相应开放记忆。
- 后端校验参与者、时间、地点和原文引用，不允许模型引用不存在的回合或实体。
- 最终保存的长期记忆使用稳定 `memory_id`，并关联本轮或连续场景的 `source_turn_ids`。

模型给出的记忆内容是提取建议，后端仍需根据本轮权威状态补齐时间、地点、人物 ID 和来源回合。

## 6. Prompt 方案

### 6.1 每轮输入内容

每轮发送给 LLM 的上下文分为以下部分：

1. 输出协议和 AI 禁令。
2. 子世代世界规则。
3. 当前时代和临近原著节点。
4. 玩家完整角色信息。
5. 玩家完整当前状态。
6. 当前存档中的完整人物关系数据。
7. 所有已生成 NPC 的档案和当前状态。
8. 当前地点和可见环境。
9. 当前时间、日程、通知和待处理事件。
10. 当前世界线偏移率和已改写节点。
11. 最近 10 个叙事回合的完整原文，受 token 上限约束。
12. 当前章节、学期和学年的阶段摘要。
13. 后端自动召回的长期事件记忆。
14. 所有必须持续传入的开放伏笔、承诺和未解决后果。
15. 玩家本轮行动。

### 6.2 完整状态的组织方式

不建议将状态拼成一段自然语言。应使用结构化 JSON 分区，例如：

```json
{
  "player": {
    "identity": {},
    "background": {},
    "vitals": {},
    "attributes": {},
    "skills": {},
    "inventory": [],
    "current_context": {}
  },
  "relationships": {
    "player_to_npc": [],
    "npc_to_npc": []
  },
  "npcs": [],
  "timeline": {},
  "pending_events": [],
  "story_summaries": {},
  "memory_context": {
    "always_included": [],
    "automatically_retrieved": [],
    "available_memory_ids": []
  },
  "worldline": {}
}
```

这样既能保证每轮提供完整信息，也便于在模型输出前后做字段级校验。

### 6.3 上下文长度控制

完整当前状态每轮都发送；历史和记忆采用：

- 默认最近 10 个叙事回合全文，并设置独立 token 上限。
- 当前章节摘要。
- 当前学期关键事实。
- 不可丢失的关系、秘密、承诺和后果。
- 后端按当前人物、地点、物品、任务和关键词自动召回 3–8 条长期记忆。

如果完整 NPC 数据在后期过大，V1 仍优先保留当前存档的完整结构化档案；叙事长文本可以只发送摘要。不能因为摘要而丢失姓名、身份、关系、性格、目标和已确认秘密等事实字段。

### 6.4 长期记忆召回流程

每个剧情回合按以下顺序执行：

1. 后端读取权威当前状态。
2. 获取最近 10 个叙事回合原文，并按 token 上限裁剪最早内容。
3. 读取章节、学期和学年摘要。
4. 从玩家行动和当前场景提取 NPC、地点、物品、任务和关键词。
5. 召回当前开放记忆以及相关度最高的 3–8 条长期记忆。
6. 第一次调用 LLM。
7. 若返回正式剧情，则进入校验和状态提交。
8. 若返回 `memory_request`，读取指定记忆关联的完整原文，再调用一次 LLM。
9. 第二次调用必须返回正式剧情；否则本回合失败且不修改存档。

自动召回的排序因素包括：

- 当前场景中出现的 NPC。
- 当前地点和物品。
- 玩家本轮文本中提到的实体和关键词。
- 当前任务、原著节点和世界线节点。
- 未解决伏笔。
- 当前关系档案引用的关键事件。
- 事件重要度和最近一次相关互动时间。

模型主动补查用于获取精确旧对话和细节，不能替代后端自动召回，因为模型无法意识到自己完全遗忘的事件。

## 7. 年龄与恋爱规则

这是 V1 的硬规则，应由后端关系系统执行，LLM 只负责表现：

### 7.1 年龄区间

设玩家和 NPC 的实际年龄由当前游戏日期和生日计算，不以“年级”代替年龄。

- `< 12 岁`：允许认识 NPC、互动、提升好感度和发展友谊，但不能进入恋爱状态。
- `>= 12 岁且 < 18 岁`：允许恋爱，但不允许进入恋爱之外的更高阶段。
- `>= 18 岁`：满足关系和好感条件后，可以进入后续阶段。

“更进一步”在 V1 中定义为：

- 不出现性行为、性暗示或成人性内容。
- 不进入需要成年条件的亲密阶段。
- 只允许适龄的情感表达、牵手、拥抱、约会和非露骨恋爱叙事。

### 7.2 延迟转入

当角色未满足年龄条件但好感度达到下一阶段阈值时：

- 系统记录 `pending_stage_unlock`。
- 好感度仍可继续变化，但不会越级显示为受限制阶段。
- 年龄达到 12 岁时，自动检查并转入允许的恋爱状态。
- 年龄达到 18 岁时，自动检查并转入此前已达到要求的下一阶段。
- 转入事件写入纪事，说明“年龄限制解除”和此前积累的条件。

### 7.3 NPC 年龄和安全检查

- 未成年玩家不能与明显不适龄的成年 NPC 进入恋爱路线。
- NPC 表白触发前必须检查双方年龄、身份和时代。
- 关系阶段变化前由后端校验年龄限制。
- LLM 即使输出违规关系状态，后端也必须拒绝并改写为合法状态。
- Prompt 中明确禁止成人化描写和越过年龄边界的内容。

## 8. 全系统 V1 基础实现

### 8.1 角色创建

实现文档中的 13 轮流程：

- 后端保存创建草稿。
- 每轮返回合法选项。
- 支持返回上一步修改。
- 最终确认后生成完整 `PlayerState`。
- 根据时代、血统、生日、天赋和宠物前置条件校验选项。
- 生成初始 NPC 和初始关系。

### 8.2 时间和事件

- 分钟级时间戳。
- 学年、学期、星期、时段和节日。
- 满月、考试周、圣诞假期等特殊标记。
- 行动按动作类型消耗时间。
- 快进一天和快进一周生成事件摘要。
- 快进期间结算课程、信件、NPC 日程和待处理事件。

### 8.3 课堂与技能

V1 接入必修课和三年级后的选修课数据：

- 签到。
- 教授提问。
- 实践操作。
- 同桌互动。
- 魔药事故、决斗练习、温室意外和夜间天文观测等事件模板。
- 技能熟练度和成绩变化。
- 考试周和学期成绩。

### 8.4 NPC 与关系

- 导入子世代主要 NPC 档案。
- 实现完整玩家-NPC关系表。
- 实现可用 NPC 的关系阶段和好感区间。
- 实现 NPC-NPC 关系的基本记录。
- 实现按日期和场景结算的 NPC 日程。
- V1 动态 NPC 先限制数量和档案复杂度，保证生成后可正常保存和追踪。

### 8.5 恋爱

- NPC 主动发起表白。
- 玩家只能接受、拒绝或等待。
- 好感、暧昧持续时间、浪漫事件次数和情境作为触发条件。
- 接受、拒绝、等待和分手都有纪事。
- 年龄规则优先级高于普通好感阈值。
- 不实现成人内容。

### 8.6 声望与舆论

- 学院、学术、社交、战斗、道德、领导、黑魔法等维度。
- 玩家行为产生声望差异。
- 恋爱公开、跨学院、跨阵营等事件产生舆论。
- 舆论作为待处理事件或剧情素材传入 LLM。
- V1 先实现文本传闻和数值变化，不实现复杂传播图算法。

### 8.7 世界线

- 保存当前偏移率。
- 每轮接收并展示 LLM 返回的偏移率、变化量和原因。
- 记录受影响的原著节点。
- 关键节点状态支持：未接近、接近中、发生、改写、产生后果、已结算。
- 玩家干预原著节点后创建世界线事件。
- 后续剧情提示词包含已经改写的节点和未解决的后果。

### 8.8 冲突与死亡

- 遭遇节点可以由事件模板或 LLM 提出。
- 进入危险节点时先保存快照。
- 由规则模块处理基础命中、伤害、资源和逃脱。
- LLM 描述战斗过程和后果。
- HP 归零先进入濒死/昏迷流程。
- 结合伤害类型、救援、地点和剧情条件判定死亡。
- 死亡后存档保持可读，可以查看结局或复制死亡前快照。

### 8.9 收藏、CG、成就和回忆

V1 不生成图片，将 CG 实现为：

- CG 编号。
- 标题。
- 触发条件。
- 解锁时间。
- 完整叙事文本。
- 关联 NPC、地点和事件标签。

收藏、成就和回忆均以结构化记录和文本详情实现。

### 8.10 查询菜单和作弊

原设定中的斜杠指令用于纯 LLM 对话场景。V1 已是完整网页游戏，因此不再向 LLM 发送或要求解析 `/状态`、`/关系` 等指令。

所有查询功能由前端菜单直接调用后端只读 API：

- 角色：身份、属性、技能、物品、魔杖、宠物和生命状态。
- 纪事：玩家选择、关键剧情、世界线变化和结局。
- 恋爱：当前恋爱状态、表白等待、年龄锁和阶段锁。
- 好感：已认识 NPC 的好感度、关系阶段和最近重要互动。
- 关系网络：玩家-NPC及已建立的 NPC-NPC 关系。
- 声望与舆论：各维度声望、传闻和影响。
- 时间与日程：当前时间、课程安排和预定事件。
- 地图：当前地点、可前往地点和解锁条件。
- 通知与信件：待办、未读信件、查看和回复入口。
- 课程与成绩：课表、选修课、熟练度和考试结果。
- 收藏、回忆、CG 和成就。
- 世界线：偏移率、变化记录和已改写节点。

建议提供以下只读接口：

```text
GET /api/sessions/{session_id}/character
GET /api/sessions/{session_id}/journal
GET /api/sessions/{session_id}/romance
GET /api/sessions/{session_id}/relationships
GET /api/sessions/{session_id}/reputation
GET /api/sessions/{session_id}/schedule
GET /api/sessions/{session_id}/map
GET /api/sessions/{session_id}/notifications
GET /api/sessions/{session_id}/letters
GET /api/sessions/{session_id}/courses
GET /api/sessions/{session_id}/collections
GET /api/sessions/{session_id}/worldline
```

菜单查询具有以下约束：

- 不调用 LLM。
- 不生成剧情回合。
- 不推进游戏时间。
- 不写入最近 10 轮叙事窗口。
- 不创建剧情纪事或长期事件记忆。
- 只读取已经由后端保存的权威状态。

信件回复、选择课程、喂养宠物等会改变状态的菜单操作，应调用专用后端动作 API。后端完成规则校验并记录系统事件；只有当该动作需要剧情表现时，才将它转换为下一次 LLM 剧情行动。

作弊入口放在设置或开发菜单中，不通过聊天文本解析。V1 先实现基础属性、熟练度、金钱、时间、世界线、好感、无敌、全知和触发剧情。所有作弊写入审计日志，并在存档状态中标记 `cheat_mode = true`。

## 9. 前端 V1 页面和交互

### 9.1 存档页

- 新建人生。
- 继续游戏。
- 重命名。
- 复制存档。
- 删除存档。
- 查看死亡或结局。
- 导出/导入本地存档。

### 9.2 角色创建页

- 显示当前轮次和进度。
- 展示可选项、前置条件和简短说明。
- 支持回退修改。
- 最终确认页展示完整角色卡。

### 9.3 主游戏页

布局建议：

```text
┌─────────────────────────────────────┐
│ 存档名 | 日期时间 | 年级 | 世界线偏移率 │
├───────────────┬─────────────────────┤
│ 角色/纪事侧栏   │ 剧情叙事区            │
│ 可折叠          │ 流式显示主持人文本     │
│                 │                     │
├───────────────┴─────────────────────┤
│ 选项 1 | 选项 2 | …… | 其他自由输入     │
└─────────────────────────────────────┘
```

角色和纪事默认可见但可折叠。主界面提供统一菜单按钮，打开角色、纪事、恋爱、好感、关系、声望、课程、信件、收藏和世界线等查询面板。

菜单面板从后端读取结构化状态，不向剧情输入框插入指令，也不会出现在 LLM 上下文或回合记录中。这使 LLM 回复只需处理剧情正文、选项、状态建议、世界线和记忆更新。

### 9.4 状态展示

首屏固定展示：

- 当前日期、时间、地点。
- HP、MP、SP、精力和饱食度。
- 当前年级。
- 世界线偏移率。
- 未读通知数量。
- 是否处于危险、濒死、表白等待或关键剧情状态。

详细的完整角色数据放在角色面板中。

### 9.5 纯文本视觉风格

- 不依赖角色立绘和图片。
- 使用学院色、羊皮纸、墨水、边框和排版营造氛围。
- 关键事件使用纯文本卡片、图标、标题和状态标签。
- CG、回忆和纪事使用不同的文本排版层级。
- 叙事内容支持流式显示和回看。

## 10. 本地配置和数据安全

### 10.1 LLM 配置

本地设置支持：

- API Base URL。
- API Key。
- 模型名称。
- 温度。
- 最大输出长度。
- 请求超时。
- 是否支持 JSON Schema。
- 是否支持流式输出。
- 是否为本机模型。

API Key 只保存在本机配置，不发送给前端页面之外的任何服务。设置界面提供“测试连接”功能，但测试请求不应携带完整游戏存档。

### 10.2 本地存档

- SQLite 数据库存放在用户数据目录。
- 设定资料和存档分离。
- 自动保存每轮完成后的完整快照。
- 启动时创建数据库备份。
- 提供 JSON/ZIP 导出。
- 导入时校验版本、文件完整性和存档 ID。
- 不通过日志打印完整 Prompt 中可能包含的敏感自定义信息。

### 10.3 网络行为

V1 默认只允许：

- 浏览器访问本机 FastAPI。
- FastAPI 请求用户配置的 LLM API。

不实现任何自动上报、遥测、远程存档、在线统计或第三方账号。

## 11. V1 实施顺序

### 阶段 A：项目骨架和运行闭环

目标：本地启动后能打开 React 页面，并由 FastAPI 提供 API。

任务：

1. 初始化前后端目录。
2. 建立 FastAPI、React、TypeScript 和 SQLite 基础工程。
3. 建立本地配置读取。
4. 实现数据库初始化和迁移。
5. 实现本地启动脚本。
6. 实现健康检查和错误页。

验收：

- 一条命令启动本地服务。
- 浏览器可访问游戏页面。
- 数据库自动创建。
- 重启服务后本地配置和存档仍存在。

### 阶段 B：会话和 13 轮角色创建

目标：能够创建一名完整的子世代玩家角色。

任务：

1. 建立 `GameSession`、`PlayerState` 和创建草稿。
2. 导入子世代角色创建选项。
3. 实现 13 轮向导。
4. 实现条件校验、回退修改和最终确认。
5. 生成基础 NPC 和初始关系。
6. 保存初始快照。

验收：

- 创建结果可刷新后继续。
- 非法选项不能提交。
- 修改前面选项后，后续依赖选项会重新校验。
- 初始角色信息可完整显示在角色面板。

### 阶段 C：第一轮 LLM 剧情闭环

目标：完成“玩家行动—LLM—选项—存档”的核心循环。

任务：

1. 实现 OpenAI-compatible Provider。
2. 定义 LLM JSON Schema。
3. 实现完整状态 Prompt Builder。
4. 实现回合请求、超时、重试和失败恢复。
5. 实现选择提交和自由文本提交。
6. 实现 SSE 或等价流式回传。
7. 强制补充“其他”选项。
8. 保存完整回合日志和快照。
9. 实现最近 10 个叙事回合原文窗口和 token 上限。
10. 实现每轮极简总结和 `MemoryEvent` 保存。
11. 实现 SQLite 标签/关键词自动召回。
12. 实现 `memory_request` 单次补查和防循环限制。

验收：

- 每轮都把完整结构化角色和关系状态传给模型。
- 模型返回非法 JSON 时不会损坏存档。
- 重复点击不会推进两次。
- 刷新页面可以恢复上一轮结果。
- 每轮都有可选选项和“其他”输入。
- 每轮正式回复都包含极简剧情总结。
- 重要事件可以建立带原文引用的长期记忆。
- 自动召回和模型补查都不会重复推进游戏状态。

### 阶段 D：基础规则和全部系统接入

目标：让所有 V1 系统都进入主循环。

按以下顺序接入：

1. 时间、地点和学年。
2. 属性、技能和物品。
3. 课程、成绩和通知。
4. NPC 状态、关系和日程。
5. 恋爱和年龄限制。
6. 声望和舆论。
7. 世界线偏移率。
8. 信件、宠物、收藏、CG 和成就。
9. 冲突、伤势和死亡。
10. 动态 NPC、血缘和作弊。
11. 前端统一查询菜单和各模块只读 API。

验收：

- 任意系统变化都可以在回合差异中找到来源。
- 角色面板和纪事面板在每轮后同步。
- 世界线偏移率每轮更新并展示。
- 年龄限制不能被模型输出绕过。
- 死亡状态后不能继续普通行动。
- 菜单查询不调用 LLM、不推进时间、不进入剧情窗口。

### 阶段 E：内容填充和游玩测试

目标：让子世代主线具备持续游玩的内容密度。

任务：

- 导入主要教师和学生。
- 编写七学年的关键节点配置。
- 为每个学年准备课程、节日和支线事件。
- 为主要 NPC 增加日程、秘密、送礼偏好和关系网络。
- 编写战斗、伤势、表白、拒绝和死亡场景。
- 加入纪事、回忆和成就的文本内容。
- 通过多次独立存档测试不同选择。

验收：

- 至少可以连续游玩一个学年。
- 至少存在一条主线、一条关系线、一条课程线和一条自由探索线。
- 至少存在一次危险节点、一次世界线改变和一种结局。
- 不同存档之间状态完全隔离。

### 阶段 F：稳定性和 V1 发布

任务：

- 规则单元测试。
- API 集成测试。
- 前端交互测试。
- 模型异常响应测试。
- 存档导入导出测试。
- 回合幂等和断线恢复测试。
- Prompt 和内容版本记录。
- 本地安装与升级说明。

## 12. V1 验收标准

### 必须满足

- 可以在本地启动和运行。
- 不需要账号和云服务。
- 可以配置任意 OpenAI-compatible 模型，或连接本机模型。
- 可以完成 13 轮角色创建。
- 可以创建、加载、复制、删除和导出存档。
- 可以连续完成至少 50 个回合。
- 每轮向 LLM 传入完整当前角色状态和完整关系状态。
- 每轮传入最近 10 个叙事回合原文，超过 token 上限时安全裁剪。
- 每轮正式回复生成极简剧情总结。
- 重要事件能够生成结构化长期记忆并关联原始回合。
- 后端能够自动召回相关长期记忆。
- LLM 能够在正式回复前发起至多一次内部记忆补查。
- 每轮返回合法选项，最后一个为“其他”。
- 每轮返回世界线偏移率固定字段。
- 前端显示世界线偏移率。
- 角色面板和纪事面板可查看。
- 角色、纪事、恋爱、好感、关系、声望、课程、信件、收藏和世界线通过前端菜单查询。
- 查询菜单不依赖斜杠指令或 LLM 解析。
- 关键状态不会因模型输出漂移或页面刷新丢失。
- 年龄和恋爱限制由后端强制执行。
- 可以处理至少一种冲突、伤势、死亡或结局流程。
- LLM 失败时存档不会被破坏。

### V1 可以不完善

- NPC 数量和动态自治深度。
- 所有原著节点的完整分支。
- 复杂的 NPC-NPC 社交网络模拟。
- 完整职业生涯。
- CG 文本数量。
- 世界线偏移率的精确数学模型。
- 叙事质量在所有模型上的一致性。

这些内容在实际游玩测试中逐步完善，但不能影响主循环、存档和状态一致性。

## 13. 测试重点

### 13.1 状态一致性

- 每轮前后状态版本正确递增。
- 同一动作重复提交只生成一个回合。
- LLM 失败不产生部分状态。
- 状态差异可以还原快照。
- 存档复制不会共享可变对象。

### 13.2 完整状态 Prompt

固定测试一个包含大量属性、物品、NPC 和关系的存档，检查：

- 所有角色身份字段都进入 Prompt。
- 所有当前状态字段都进入 Prompt。
- 所有当前关系记录都进入 Prompt。
- 已生成 NPC 的档案不会因为不在最近剧情中而丢失。
- 近期历史和长期摘要不会覆盖结构化事实。

### 13.3 长期事件记忆

至少覆盖：

- 最近 10 个叙事回合按正确顺序进入 Prompt。
- 菜单查询和系统设置不会进入最近 10 轮窗口。
- 超过 token 上限时优先裁剪最早的叙事回合。
- 普通日常只保存总结和原文，不创建永久记忆。
- 重要承诺、秘密、物品和关系事件会建立长期记忆。
- `source_turn_ids` 能正确取回完整原文。
- 当前 NPC、地点、物品和开放伏笔可以触发自动召回。
- 模型请求不存在的记忆 ID 时安全失败，不允许猜测原文。
- 每个玩家回合最多补查一次。
- 补查过程不推进时间、不修改状态、不生成纪事。
- 补查后的第二次响应仍不是正式剧情时，整个回合不提交。
- 已解决伏笔会更新原记忆状态，且不会被当成仍开放事件反复触发。

### 13.4 查询菜单

- 所有查询菜单都直接读取后端权威状态。
- 打开和关闭菜单不调用 LLM、不创建 `TurnRecord`。
- 查询不会推进时间、消耗属性或改变世界线。
- 菜单展示值与回合提交后的最新状态一致。
- 有状态变更的菜单操作经过专用动作 API 和规则校验。
- 菜单操作不会被错误拼接到剧情 Prompt。

### 13.5 年龄和恋爱

至少覆盖：

- 11 岁达到恋爱好感线但保持友谊。
- 12 岁时自动转入允许的恋爱状态。
- 17 岁达到下一阶段要求但保持当前阶段。
- 18 岁时自动转入下一阶段。
- 玩家和 NPC 年龄不满足条件时拒绝表白或恋爱状态。
- 模型返回违规关系状态时被后端拦截。

### 13.6 世界线

- 无变化时返回并显示相同偏移率。
- 玩家改变原著节点后返回变化量和原因。
- 偏移率越界时被校验。
- 世界线说明被写入纪事。
- 后续 Prompt 能读取此前已改写节点。

### 13.7 LLM 故障

- API Key 错误。
- Base URL 错误。
- 模型不支持 JSON Schema。
- 返回空文本。
- 返回非法 JSON。
- 返回缺少“其他”的选项。
- 超时。
- 限流。
- 网络中断。

## 14. V1 不做的事情

为了保持本地单人产品边界，V1 不做：

- 账号系统。
- 云端同步。
- 多人游戏。
- 在线排行榜。
- 自动遥测和用户行为上报。
- 图像生成。
- 角色立绘和图片 CG。
- 官方图片、电影片段和原著大段文本的内置资源。
- 微服务拆分。
- 复杂向量数据库依赖。
- 让模型直接执行数据库或系统命令。

## 15. 开发前仍需确认的少量参数

整体设计已经可以开始实施，剩余问题不会阻塞项目骨架。建议在真正编码前确定：

- 默认使用哪一个 OpenAI-compatible 模型作为开发测试模型。
- API Key 是只通过本地配置文件设置，还是增加设置页面。
- 默认数据目录采用项目内 `data/`，还是操作系统用户数据目录。
- 是否需要做成“一键启动脚本”，还是接受分别启动前后端。
- 世界线偏移率显示为整数百分比，还是保留一位小数。
- V1 是否默认打开作弊入口，还是需要在设置中手动启用。

## 16. 第一批可直接执行的任务

建议下一步按以下顺序开始编码：

1. 创建 React + TypeScript + Vite 前端工程。
2. 创建 FastAPI + Pydantic + SQLAlchemy 后端工程。
3. 实现本地配置读取和 SQLite 初始化。
4. 建立 `GameSession`、`PlayerState`、`NPCState`、`Relationship`、`TurnRecord`、`MemoryEvent`、`StorySummary` 和 `JournalEntry`。
5. 建立子世代的时代配置和 13 轮角色创建配置。
6. 实现创建向导和初始状态快照。
7. 实现 OpenAI-compatible Provider 和连接测试。
8. 实现固定 Prompt 结构，确认每轮包含完整当前状态。
9. 实现正式剧情与 `memory_request` 两类互斥 JSON Schema、校验和重试。
10. 实现最近 10 轮原文窗口、每轮极简总结、长期记忆和阶段摘要。
11. 实现后端自动召回和模型单次原文补查。
12. 实现世界线固定字段和前端显示。
13. 实现主游戏页面、统一查询菜单、角色面板和纪事面板。
14. 为恋爱、好感、关系、声望、课程、信件、收藏和世界线实现查询 API。
15. 接入时间、关系、恋爱年龄限制和最小事件系统。
16. 为其余系统建立基础接口和最小内容。
17. 使用多个本地存档进行连续游玩测试。

完成第 1–12 项即可验证最关键的技术风险：本地运行、模型兼容、完整状态传递、长期记忆、结构化返回、世界线展示和存档可靠性。完成第 13–17 项后，进入全系统内容完善阶段。

